# Bitácora de avance — EV2 Clandestinoz

Cada fila se agrega al terminar y verificar un paso del manual. Formato: fecha · paso · qué se hizo · verificación · commit/PR.

| Fecha | Paso | Qué se hizo | Verificación | Commit / PR |
|---|---|---|---|---|
| 2026-09-03 | — | Plan (`PLAN_IMPLEMENTACION_Y_DESARROLLO.md`) y manual (`MANUAL_IMPLEMENTACION_EV2.pdf`) creados. `CLAUDE.md` y esta bitácora creados. | Archivos presentes en la carpeta. | (sin git aún) |
| 2026-09-03 | 0.1 (parcial) | `.gitignore`, `AUTHORS.md`, `git init -b main`, autor Erick Lopez, primer commit `66d5e7f` (129 archivos). `.env` y `Thumbs.db` excluidos. | `git status` limpio · `git ls-files \| grep env` → solo `.env.example` · `git fsck` sin errores. Pendiente: remoto privado y push. | 66d5e7f (local) |
| 2026-09-03 | 0.1 (cierre) | Remoto `origin` = https://github.com/Jovannyrdmx/ev2-app.git; push de `main` realizado desde la computadora del desarrollador. | `git branch -vv` → `main [origin/main]` sincronizada en `bfef436`. **Paso 0.1 completo.** | bfef436 |
| 2026-09-03 | 0.2 | Reorganización a `server/ web/ mobile/ deploy/ docs/` con `git mv` (rama `fase0/reorganizar`, commit `253c35d`). `README.md` nuevo. Sin cambios de contenido en código. | `git status` limpio · 123 renombrados, 5 nuevos (README + 4 .gitkeep), 0 borrados, 0 modificados · `git log --follow` conserva historial · raíz solo con README, CLAUDE, AUTHORS, .gitignore, .env.example y carpetas. | 253c35d (rama fase0/reorganizar) |
| 2026-09-03 | 0.3 | `docs/DECISIONES.md` con 15 decisiones (qué, por qué, alternativas descartadas) y reglas absolutas; enlazado desde `README.md`. | Archivo existe y `grep DECISIONES README.md` lo muestra. | rama fase0/decisiones |
| 2026-09-03 | 0.4 | `server/package.json` reescrito (nombre `ev2-server`, scripts reales, engines node>=18, jest config); dependencias añadidas: stripe, mercadopago, dotenv, helmet, express-rate-limit, zod, pino, pino-http; dev: jest, supertest, eslint 9 (+@eslint/js, globals), nodemon. `eslint.config.js` creado. `package-lock.json` generado (508 paquetes). | `npm ls --depth=0` sin errores. `npm run lint`: 26 errores, todos en `src/routes/reservations.js` (`pool`, `authenticateToken`, `broadcastToNightclub` no definidos: confirma que nunca se integró; se corrige en 1.4) + 14 avisos de variables sin usar en código heredado. | rama fase0/package |
| 2026-09-03 | 0.5 | `server/src/index.js`: cliente Redis con sintaxis v4 (`socket: {host, port}` + `reconnectStrategy`), `dotenv`, función `start()` que conecta Redis y verifica Postgres antes de escuchar (sale con código 1 si no hay conexión), apagado limpio con SIGTERM/SIGINT, y `module.exports` de app/server/pool para tests. | `node --check` OK · el módulo carga y exporta app/server/pool/redisClient/start · sin Redis el arranque falla limpio con exit 1 · eslint 0 errores en el archivo. Conexión real se comprueba en 0.7. | rama fase0/redis |
| 2026-09-03 | 0.6 | `deploy/`: `Dockerfile.api` y `Dockerfile.ws` reescritos (node:20-alpine, `npm ci --omit=dev`, copia `src/` y `migrations/` completos, usuario `node`, healthcheck); `Dockerfile.web` con nginx sirviendo `web/` + `nginx-web.conf` (cabeceras de seguridad, gzip); `Dockerfile.legacy` eliminado; `docker-compose.yml` nuevo (contextos correctos, Postgres/Redis sin puertos al host, `DB_PASSWORD` y `JWT_SECRET` obligatorios, sin initdb, nginx bajo `--profile proxy`); `docker-compose.override.example.yml` para exponer BD en desarrollo; `.dockerignore` en raíz y `server/`; `.env.example` con `DB_NAME=ev2` y secretos marcados como obligatorios. | `docker compose config` valida (5 servicios). No fue posible construir imágenes en la nube (registro Docker bloqueado): en su lugar se ejecutó el código real contra Postgres 16 y Redis 7 nativos: `Redis connected`, `PostgreSQL connected`, `/health` API y WS → 200, SIGTERM apaga limpio. El `docker compose build/up` se confirma en la PC del desarrollador en 0.7. | rama fase0/docker |
| 2026-09-03 | 0.7 (en curso) | Error en la PC: `DB_PASSWORD is required`. Causa: con `-f deploy/docker-compose.yml`, Compose busca `.env` en `deploy/`, no en la raíz. Corrección: usar `--env-file .env`; README y cabecera del compose actualizados. | Pendiente: build/up en la PC del desarrollador. | rama fase0/compose-envfile |
| 2026-09-03 | 0.7 | `docker compose --env-file .env -f deploy/docker-compose.yml build` y `up -d` ejecutados en la PC del desarrollador (Windows, Docker Desktop). | `http://localhost:3000/health` → 200 `{"status":"ok"}` · `http://localhost:4000/health` → 200 `{"status":"ok","clients":0}` · `http://localhost:8080/` → 200. La API arrancó, lo que implica conexión real a Postgres y Redis dentro de Docker. `compose ps`: postgres, redis, api, ws **healthy**; web `unhealthy` pese a responder 200 → healthcheck con `localhost` (IPv6) contra nginx solo IPv4. Corregido: `listen [::]:8080` + healthchecks con `127.0.0.1`. Rebuild de `web` en la PC: healthy. **Paso 0.7 completo.** | main |
| 2026-09-03 | 0.8 | `.github/workflows/ci.yml`: job `server` (Node 20, `npm ci`, lint con `continue-on-error` temporal hasta 1.4, `npm test --passWithNoTests`) y job `docker` (build de las 3 imágenes + `compose config` con secretos ficticios). Badge de CI en README. | Pendiente: primer run verde en GitHub Actions tras el push. | rama fase0/ci |
| 2026-09-03 | 0.9 (en curso) | Investigación pública: National Soft expone "SoftRestaurant Service" (REST, header `AuthorizedApp`, solo menú, llave por correo), programa oficial de integración para terceros (Deliverect/Hubster/Ordatic envían órdenes), add-ons e-Delivery/SR Móvil, módulo ERP/PMS (PDF), y SR11 corre sobre SQL Server. El cliente heredado asume rutas inexistentes. Borrador en `docs/POS_REAL.md` con escenarios A–E y recomendación (A comandas + D lectura SQL). | Pendiente: datos del dueño (versión, licencias, credenciales SQL/API), envío del formulario a National Soft, lectura del PDF ERP/PMS. | rama fase0/pos-real |
| 2026-09-03 | 0.9 (cierre) | Confirmado con el desarrollador: SR **11.0.98**, SQL Server en servidor `192.168.2.108` (LAN del club, acceso por Radmin VPN), PC del desarrollador = cliente contra datos reales, sin ambiente de pruebas, conexión cifrada en `conexionDB.dbl` (no se toca). `docs/POS_REAL.md` cerrado para Fase 0; **D16 agente local** añadida a `docs/DECISIONES.md`. Pendientes no bloqueantes: formulario a National Soft, PDF ERP/PMS, add-ons, nombre de instancia/base, administrador del servidor. **FASE 0 COMPLETA.** | `docs/POS_REAL.md` secciones 3–5; D16 en DECISIONES. | rama fase0/pos-real-final |
| 2026-09-03 | 1.1 | `server/migrations/001_init.sql`: esquema unificado, **38 tablas** (núcleo, mesas, bebidas/pedidos, flirt y seguridad, finanzas con libro `transactions` inmutable, propinas/canciones/turnos, reservaciones con exclusión de solapes, taxi/valet, precios/POS/eventos), extensiones pgcrypto y btree_gist, triggers `updated_at`, `forbid_delete`, guardia de columnas inmutables y cálculo de `ends_at`. Diseño aprobado por el dueño antes de escribir. | Aplicada en PostgreSQL 16 limpio sin errores. Pruebas: solape de reservaciones rechazado; cambio de estado en `transactions` OK y cambio de monto/borrado rechazados; segunda ocupación abierta rechazada; flirt a sí mismo y rol inválido rechazados. | rama fase1/esquema |
| 2026-09-03 | 1.2 | `src/db/pool.js` (pool compartido, `DATABASE_URL` o `DB_*`), `src/db/migrate.js` (runner sin dependencias: tabla `schema_migrations`, transacción por archivo, `pg_advisory_lock`, `--status`), `seeds/run.js` (idempotente; club, 20 mesas con layout, 30 bebidas + inventario, 10 usuarios uno por rol con bcrypt, reglas, 3 descuentos, 4 reglas de precio, 4 contactos, 12 lugares de valet, tipo de cambio; se niega en producción o sin `SEED_PASSWORD`). `index.js` usa el pool compartido y Redis abandona tras ~1 min sin conexión. Compose: `api` ejecuta `migrate` antes de arrancar. `.env.example`: `SEED_PASSWORD`. `001_init.sql` sin BEGIN/COMMIT (transacción la maneja el runner). | `migrate` ×2 (segunda: nada que aplicar), `--status` OK, `seed` ×2 sin duplicados (20/30/10/4/12), 39 tablas, seed rechazado con `NODE_ENV=production` y sin `SEED_PASSWORD`, API arranca con el pool compartido y `/health` 200. Lint: 0 errores nuevos. | rama fase1/migraciones |
| 2026-09-03 | 1.3 | API refactorizada por módulos contra el esquema nuevo: `src/app.js` (fábrica de la app: helmet, CORS estricto, pino-http con id de petición y redacción de secretos, rate limit 300/min y 10/min en auth, JSON 1 MB), `src/index.js` (solo arranque, verifica JWT_SECRET/BD/Redis y apaga limpio), `src/db/redis.js`, middleware `errors/validate(zod)/auth(JWT 15 min + refresh rotativo revocable + requireRole + sameNightclub)`, routers `auth` (registro con 18+ y términos, login, refresh, logout, me), `nightclubs` (perfil, staff, contactos, eventos, dashboard), `drinks` (menú, categorías, inventario, alta/edición), `orders` (creación idempotente con bloqueo de inventario y verificación de stock, cola, transiciones validadas, devolución de stock al cancelar), `tables` (mapa con ocupantes, sentarse/liberar, editor de layout), `services/events.js`, y `services/pos/softrestaurant11.js` como marcador que lanza 501 (D16). Webhook POS responde 501 hasta la Fase 4. | **22 pruebas de extremo a extremo, todas verdes** (`docs/api/smoke-test.sh`) contra Postgres+Redis reales con datos sembrados: registro/menor de edad 403/contraseña débil 400, login 3 roles, credenciales malas 401, sin token 401, menú 30 y filtro, inventario 403 guest y OK bartender, 20 mesas, sentarse, pedido con idempotencia (reenvío no duplica), stock 100→98, cola, ciclo confirmed→preparing→ready→delivered, transición inválida 409, guest no cambia ajenos 403, cancelación devuelve stock, dashboard solo manager, eventos, staff/contactos, rotación de refresh (reutilizar el viejo 401), aislamiento entre clubes 403, 404 JSON. Bug encontrado y corregido en pruebas: `FOR UPDATE` sobre el lado nulo de un LEFT JOIN (Postgres 0A000). Lint: 0 errores. | rama fase1/api-modular |
| 2026-09-03 | 1.4 | `src/routes/reservations.js` reescrito desde cero contra el esquema nuevo y **montado** en la app: reglas del club, disponibilidad calculada con `reservations`+`tables`, cotización con `pricing_rules` + extras + descuentos, reserva idempotente con depósito calculado en servidor, agenda del staff, ciclo confirmed→seated→completed/no_show, cancelación con política de reembolso por ventanas, y métodos de pago (solo tokens, nunca PAN). Nuevo `src/services/pricing.js`. El cobro real llega en la Fase 3: la reserva nace `pending_payment` con transacción `pending`. **Retirado el `continue-on-error` del lint en CI.** | **25 pruebas de extremo a extremo verdes** (`docs/api/smoke-test-reservations.sh`): reglas, 403 a guest, disponibilidad (8 mesas, precio 1500, depósito 450), 400/422 por esquema y reglas del club, cotización con aritmética verificada (2970/891), código inválido 422, reserva idempotente, **solape 409**, mesa excluida de disponibilidad, capacidad 422, flujo de estados, transición inválida 409, rol incorrecto 403, cancelar completada 409, reembolso 100% (>48h) y 0% (<24h), aislamiento 403, agenda, métodos de pago sin exponer el token. Libro contable verificado en BD. Lint: **0 errores en todo `src`**. | rama fase1/reservaciones |
| 2026-09-03 | 1.5 | `server/openapi.yaml` (OpenAPI 3.1): **41 operaciones** en 36 rutas, 29 esquemas reutilizables, seguridad Bearer, parámetros y respuestas comunes, `operationId` único por operación (necesario para generar los clientes iOS/Android en la Fase 6) y documentación en español de autenticación, formato de errores, manejo de dinero como cadena decimal e idempotencia. Servido en `/api/docs` con swagger-ui-express (desactivable con `SERVE_API_DOCS=false`) y el YAML crudo en `/api/openapi.yaml`; si el archivo faltara, la API arranca igual. `redocly.yaml` con las tres excepciones justificadas por comentario. Nuevo script `npm run lint:api`, añadido al CI. | `redocly lint` → **válido, 0 errores y 0 avisos**. `/api/docs` responde 200 y muestra las 8 secciones (captura revisada). `/api/openapi.yaml` se sirve correctamente. Regresión completa sin fallos: 22 pruebas de pedidos y 25 de reservaciones verdes con base limpia. Lint de código: 0 errores. | rama fase1/openapi |
| 2026-09-03 | 1.6 | Suite automatizada con jest + supertest: **144 pruebas** en 5 archivos (`auth`, `orders`, `tables`, `reservations`, `catalog`) sobre PostgreSQL real, importando `app.js` sin abrir puertos; helpers de ciclo de vida de BD (migrar una vez, TRUNCATE entre suites) y fábricas de datos. CI actualizado con servicio Postgres y `npm run test:coverage`; umbral de cobertura fijado en `src/routes` (70% sentencias). **Dos errores reales encontrados y corregidos:** (1) `transactions.direction` era `CHAR(3)` y Postgres rellenaba `'in'` como `'in '`, rompiendo comparaciones en cualquier cliente → migración `002_fix_direction_padding.sql` (probada sobre una BD con datos previos) y corrección del esquema base; (2) al mudarse de mesa, la anterior seguía marcada como ocupada aunque quedara vacía → el mapa mostraba mesas fantasma toda la noche. Además dos inconsistencias de contrato: importes dentro de arreglos JSON salían como número en vez de cadena de dos decimales (`::text` en pedidos y extras) y los ids de evento son enteros de 64 bits que viajan como cadena (documentado en OpenAPI). | `npx jest`: **144 pruebas, 5 suites, todas verdes**. Cobertura `src/routes`: **94.25% sentencias, 86.99% ramas** (objetivo del manual: 60%). `redocly lint`: válido. Migración 002 verificada sobre datos existentes: `'in '` → `'in'`. | rama fase1/tests |
| 2026-09-03 | 2.1 | **Hallazgo:** `nightclub-tables.js` no era código muerto sino el **plano real del club** (52 mesas con número, zona, piso, capacidad y coordenadas; 276 lugares; 6 áreas: barra, pista, DJ, VIP, entrada, baños). Confirmado vigente por el dueño. Se detectaron números repetidos (16, 17 y 18 existían en VIP Elevado y en Zona Azul): por indicación del dueño se distinguen con prefijo de zona (**V16/V17/V18** y **A16/A17/A18**), lo que evita entregar bebidas en la mesa equivocada. Migración `003_floor_plan.sql`: columnas `floor` (el club tiene dos plantas, el esquema asumía una), `table_number` (el personal dice "la mesa 39", no un UUID) y `color`; nueva tabla `venue_landmarks` para lo que no es mesa (antes iba mezclado y falseaba todo conteo de mesas). Plano extraído a `seeds/data/ev2-floor-plan.json` y cargador `npm run seed:floor` (idempotente; las mesas ausentes del plano se **retiran**, no se borran, para no perder su historial). Nuevos endpoints `GET /floor-plan` (mesas + áreas + lienzo + ocupación, filtrable por piso) y `GET /tables/stats` (aforo total y desglose por piso y zona con porcentajes, vía ROLLUP). Listado de mesas ahora expone piso, número y color y se ordena como el personal las nombra. Eliminadas `dance-floor-manager.js` y `nightclub-tables.js`. | **166 pruebas verdes** (22 nuevas en `floorplan.test.js`): 52 mesas/276 lugares cargados, prefijos correctos (V=10 lugares, A=4), 40 abajo y 12 arriba, recarga idempotente, **recargar el plano no borra la ocupación en curso**, mesas retiradas quedan inactivas, sin códigos duplicados, plano filtrable por piso con áreas comunes visibles, ocupación en vivo, porcentajes (1 de 3 mesas VIP = 33%; 1 de 30 lugares = 3%), fuera de servicio contado aparte, 403 a clientes. Cobertura `src/routes` 94.45%. `redocly lint` válido (43 operaciones). | rama fase2/mesas |
| 2026-09-03 | 2.2 (parte 1) | **El dueño entregó el mapa y la lista de precios oficiales** (imágenes guardadas en `docs/referencia/`). El plano cargado en 2.1 estaba **desactualizado**: tenía 52 mesas y zonas inexistentes (Zona Baja, Celeste, Baja Celeste), faltaba la mesa 15 y una 13 y 14. Reconstruido a **55 mesas / 308 lugares** en 8 zonas reales (Roja, Azul, Diamante, VIP Elevado, Rosa, Doble Diamante, Celebration Suite y 28 mesas General), verificado dibujando el plano y comparándolo con la imagen. **Modelo de cobro corregido (D17):** la mesa se vende por noche, no por hora; `total = precio de zona + personas adicionales × boleto del evento`; cada zona incluye N boletos y un tope de extras; el boleto cambia por evento y el gerente puede sobrescribir cualquier zona solo para un evento; precios congelados al reservar; plazo de llegada 3 h sin reembolso. Migración `004`: `events_calendar`, `zone_pricing`, `event_zone_pricing`, `reservation_products` (6 botellas y 8 extras reales), y columnas de evento/plazo/precios congelados en `reservations` con índice único de una reservación viva por mesa y evento. Nuevos `seeds/data/ev2-price-list.json`, `npm run seed:prices` y `src/services/event-pricing.js`. | Migración aplicada; plano cargado (55 mesas, 8 áreas, 308 lugares) y lista de precios (8 zonas, 6 botellas, 8 extras). Cálculo verificado con boleto de $250: Zona Roja 8 pers = $5,000 y 9 pers **rechazada** (no admite extras); Zona Azul 10 = $4,500, 12 = $5,000 (2 × $250), 13 **rechazada** (máximo 12); Celebration Suite 10 = $8,500. Sobrescritura por evento: Zona Roja a $7,000 con 10 boletos aplica solo esa noche y **otra noche conserva $5,000 / 8 boletos / sin extras**. | rama fase2/precios-evento |
| 2026-09-04 | 2.2 (parte 2) | **Administracion de eventos y precios desde la app.** Nuevo `src/routes/events.js`: calendario de noches (crear, editar, publicar, cancelar, borrar), precio del boleto por evento, lista de precios estandar por zona (`GET/PUT /zone-pricing/:section`), ajuste de una zona **solo para un evento** (`PUT/DELETE /events/:id/zones/:section`, cada campo en `null` vuelve al estandar) y catalogo de botellas y extras (`reservation-products`). Un cliente solo ve eventos `published`; todo lo que escribe exige rol `manager`. Un evento con reservaciones vivas no se borra: se cancela (409). Cambiar el boleto o una zona con reservaciones responde con cabecera `X-Warning`. **Reservaciones reescritas al modelo por noche:** ligadas a un evento, sin horario ni duracion; disponibilidad y cotizacion calculan `base de zona + personas extra x boleto + extras`; los extras se resuelven por `code` del catalogo (el precio lo pone el servidor); al reservar se congelan `zone_base_at_booking` y `ticket_at_booking`; nuevo `POST /reservations/release-no-shows` que libera sin reembolso a quien no llego en plazo, y cancelar despues del plazo devuelve 0%. Renombrada la ruta de sincronizacion de eventos de dominio a `/nightclubs/:id/sync/events` para no chocar con el calendario. `openapi.yaml` actualizado: 54 operaciones, nueva etiqueta *Eventos y precios*, esquemas `Event`, `EventCreate/Update`, `ZonePricing`, `ZoneOverride`, `EffectiveZone`, `ReservationProduct`, `Quote` y `Reservation` rehechos; el esquema de eventos de dominio pasa a llamarse `SyncEvent`. | **209 pruebas verdes** (`npx jest`, 7 suites): 45 nuevas en `events.test.js` y 41 reescritas en `reservations.test.js`. Verificado con boleto de $250: Zona Azul 10 personas $4,500 y 12 personas $5,000 (2 x $250); Zona Roja rechaza la novena persona (no admite extras); ajuste por evento aplica solo esa noche; una mesa no se reserva dos veces en el mismo evento pero si en noches distintas; deposito 30% asentado como `pending`; reembolso 100/50/0% por ventana y **0% pasado el plazo de llegada**; liberacion de no-shows idempotente. `floorplan.test.js` actualizado al plano real (55 mesas, 308 lugares, 8 areas). `eslint` 0 errores; `redocly lint openapi.yaml` valido. | rama fase2/precios-evento |
| 2026-09-04 | 2.3 (parte 1) | **Flirt con proteccion primero (D18).** Migracion `005_flirt.sql`: `discoverable` en preferencias (aparecer en la lista es distinto de aceptar flirts), `flirts` gana idempotencia, mesa del emisor, estado (`sent/viewed/accepted/declined/expired`) y caducidad de 12 h; vocabulario cerrado de reacciones; `returned_to_sender` en pedidos y motivos fijos en reportes (para la parte 2). Nuevo `src/routes/flirts.js`: `GET/PUT /me/preferences`, catalogo de 7 emojis, `GET /flirts/people` (solo clientes sentados que quieren aparecer; sin bloqueos ni reportes abiertos; expone solo nombre, mesa y zona), `POST /flirts` (emoji y meet; trago/botella responde 501 hasta la parte 2), bandejas `received`/`sent` de la noche, `view` y `react`. Reglas en el servidor: opt-in obligatorio (403), ambos sentados (422), 20/hora (429), 3 sin respuesta a la misma persona (429), «no me interesa» silencia al emisor esa noche (403), el personal no envia, el gerente no lee bandejas. Eventos `flirt_received` y `flirt_reaction` para la fase 3. | **243 pruebas verdes** (34 nuevas en `flirt.test.js`): preferencias nacen apagadas; lista sin correo/telefono/fecha; sentado obligatorio; personal excluido; idempotencia; opt-in y apagado a media noche; limites por hora y por persona; silencio en un solo sentido; caducidad saca de la bandeja y bloquea reaccion (409); tercero y gerente reciben 404. Migracion 005 aplicada sobre 001-004 sin error. `eslint` 0 errores. | rama fase2/flirt |
| 2026-09-04 | 2.3 (parte 2) | **Trago o botella por flirt, bloqueos, reportes y panel del gerente.** Nuevo `src/services/orders.js` (creacion de pedidos compartida por la ruta de pedidos y por el flirt, misma transaccion). Un flirt `drink`/`bottle` crea un **pedido real** para la mesa del receptor, cobrado al emisor (asiento en `transactions` marcado no reembolsable) y visible en la cola de la barra; si el receptor responde «no me interesa», el pedido **vuelve a la mesa del emisor** (`returned_to_sender`, evento `order_returned` a barra y emisor) sin cancelarse ni reembolsarse; el emisor no puede cancelar una invitacion (403). Bloqueos (`GET/POST/DELETE /me/blocks`): ocultan a ambos, 404 neutro al enviar, cierran lo pendiente. Reportes (`POST /users/:id/report`, migracion `006`): motivo fijo, un abierto por par, pueden adjuntar el flirt recibido; con 2 abiertos la persona desaparece. Gerente: `GET /reports`, `PATCH /reports/:id` (`actioned` bloquea la cuenta, cierra sesiones y la saca de la mesa) y `GET /flirts/stats` (solo conteos). `openapi.yaml`: 70 operaciones, etiqueta *Flirt* y 9 esquemas nuevos. Eliminado `flirt-system.js`. | **266 pruebas verdes** (57 en `flirt.test.js`, 23 nuevas): invitacion de 2 cervezas = pedido de $240 a la mesa 39 cobrado a Ana y existencias descontadas; botella se asienta como `bottle_service`; rechazo devuelve el pedido confirmado a la mesa 5 con el asiento intacto y sin devolver existencias; rechazo tras entregado no mueve nada; sin existencias no se cobra ni se crea el flirt; idempotente (un solo cobro); bloqueo en ambos sentidos con 404 neutro; reportes: motivo invalido 400, duplicado 409, evidencia ajena 404, gerente ve el reporte sin el mensaje, `actioned` bloquea la cuenta (403 al entrar) y la saca de la mesa, `dismissed` la vuelve a mostrar; panel sin nombres ni mensajes. Cobertura `src/routes` 94%. `eslint` 0 errores; `redocly lint` valido; 70/70 rutas documentadas. | rama fase2/flirt |
| 2026-09-04 | 2.4 (parte 1) | **Empleados y cuentas bancarias (D19).** Migracion `007`: `must_change_password`, moneda de pago preferida, `routing_last4`/`verified_by`/`active` en cuentas, columnas de conversion y rechazo en retiros, indices de una cuenta predeterminada y un retiro en curso por empleado. Nuevo `src/services/banking.js` (validacion CLABE con digito verificador y routing ABA con checksum; cifrado `pgp_sym_encrypt` con `BANK_ENCRYPTION_KEY`, ahora obligatoria en `.env`, compose e `index.js`). Nuevo `src/routes/employees.js`: `GET/POST /employees`, `GET/PATCH /employees/:id` (rol, datos, baja/alta, reinicio de contrasena), `GET/PUT /employees/me`, `GET/POST/DELETE /employees/me/bank-accounts` (siempre enmascaradas), `GET /employees/:id/bank-accounts` y `POST .../verify` para el gerente. `POST /auth/password` nuevo; el middleware de autenticacion bloquea todo salvo cambiar contrasena mientras `must_change_password` este activo. | **287 pruebas verdes** (21 nuevas en `employees.test.js`): CLABE valida/alterada, ABA valido/invalido; alta devuelve contrasena temporal de 12 caracteres que no se guarda en claro; solo el gerente da de alta; roles no validos 400, correo repetido 409; con la temporal cualquier ruta responde 403 `password_change_required` hasta cambiarla; cambio exige la actual y una distinta; reinicio por el gerente cierra sesiones; baja impide entrar y reactivacion lo permite; CLABE guardada cifrada (el binario no contiene el numero, solo descifra con la llave) y la API devuelve `****xxxx`; routing cifrado; invalidas no guardan nada; una sola predeterminada; el gerente verifica viendo solo la mascara; un empleado no ve ni borra cuentas de otro; sin llave el servidor responde 500 y no guarda. | rama fase2/empleados |
| 2026-09-04 | 2.4 (parte 2) | **Saldo, retiros, tipo de cambio y nomina.** El saldo se **calcula** siempre desde `transactions` (cobrado como beneficiario, `paid`) menos retiros pendientes/aprobados (reservados) y pagados, por moneda. `GET /employees/me/earnings?from&to` (saldo + desglose por tipo y por dia), `GET /employees/me/dashboard`, `GET/POST /employees/me/withdrawals` (sin minimo; exige cuenta **verificada**, saldo suficiente y ningun retiro en proceso; bloqueo por empleado contra dobles peticiones; conversion con el tipo de cambio vigente). Gerente: `GET /withdrawals`, `approve` (fija el tipo de cambio de ese momento), `reject` (con motivo; el saldo vuelve), `paid` (asienta la salida `withdrawal` en el libro y descuenta), `GET /employees/:id/earnings`, `GET /payroll`. `GET/PUT /exchange-rate` con historial (fecha y autor; nunca se sobreescribe). `openapi.yaml`: 94 operaciones, etiqueta *Empleados*, 10 esquemas nuevos, `POST /auth/password` documentado. `Dockerfile.api` ahora copia `openapi.yaml` (antes `/api/docs` fallaba en Docker). Eliminado `employee-account-system.js`. | **306 pruebas verdes** (40 en `employees.test.js`, 19 nuevas): saldo MXN y USD por separado y solo lo `paid`; filtro por fechas no altera el saldo; empleado no ve ganancias de otro (403), gerente si; tablero con saldo, movimientos, retiro abierto y tipo de cambio sin exponer la cuenta; historial de tipo de cambio con autor, empleado sin historial ni permiso; retiro de $1 aceptado (sin minimo) y reserva saldo; 1000.01 sobre 1000 rechazado (422) y 1000 aceptado; sin cuenta o sin verificar 422; segundo retiro 409; dos peticiones simultaneas dejan una sola; USD a MXN 100 x 18 = 1800 al pedir y 1850 al aprobar con 18.5; sin tipo de cambio 422; pagar antes de aprobar 409; pagado crea asiento `out` de 400 con folio y el saldo queda 600; rechazo con motivo devuelve el saldo; nomina por empleado y moneda. Cobertura `src/routes` 94%. `eslint` 0 errores; `redocly lint` valido; 94/94 rutas documentadas. | rama fase2/empleados |
| 2026-09-04 | 2.5 | **Propinas, turnos, tragos al personal y canciones (D20).** Migracion `008`: `tip_presets` (minimos/sugeridos/permiso de tragos por rol), `tips.anonymous/status/song_request_id`, `staff_drinks` con idempotencia, mesa de origen y devolucion, `song_requests` con clave normalizada y votos, `song_request_votes`. Nuevo `src/routes/tips.js` (23 rutas): presets (nacen con los montos que el club ya usaba; el gerente edita), turnos (abrir/cerrar, el gerente cierra olvidados, quien esta en turno con montos y nombre artistico), propinas (fila + asiento en una transaccion, idempotente, minimo por rol, anonimas para el empleado pero no para el gerente, confirmar efectivo y cancelar pendientes), tragos al personal (solo roles con `accepts_drinks` en turno, desde una mesa; pedido real cobrado al enviar; aceptar/rechazar con devolucion a la mesa del cliente), canciones (al DJ en turno; la misma cancion se junta con votos; propina aparte con el minimo del DJ; el DJ marca tocada y no puede rechazar; cola por votos; clientes ven la cola para votar sin montos), tablero de reconocimiento (noche/mes; clientes sin montos). `openapi.yaml`: 117 operaciones, etiqueta *Propinas y canciones*, 6 esquemas. Eliminado `tip-dancer-system.js`. | **339 pruebas verdes** (33 nuevas en `tips.test.js`): presets iniciales correctos y editables (sugerido < minimo → 422); turnos sin duplicar y cierre por el gerente; propina crea `tips` + `transactions` en pending; idempotencia (2 POST → 1 fila y 1 asiento); el libro rechaza `UPDATE` del monto y `DELETE`; minimo por rol y minimo editado; pendiente no entra al saldo y confirmada si; cancelar solo pendientes; anonimato respetado para el empleado y visible al gerente; trago a mesero rechazado hasta que el gerente autoriza el rol; fuera de turno o sin mesa 422; rechazo devuelve el pedido a la mesa 5 con asiento intacto; «La Chóna!!» y «la chona» se juntan (2 votos, 1 fila); misma persona dos veces 409; propina en una solicitud existente queda al DJ; cola por votos, tocada avisa a los dos que la pidieron, `decline` no existe (404); otro DJ 404; clientes sin montos ni ids; tablero ordenado por cobradas y sin montos para el cliente. Cobertura `src/routes` 94%. `eslint` 0 errores; `redocly lint` valido; 117/117 rutas documentadas. | rama fase2/propinas |
| 2026-09-04 | 2.6 | **Salida segura con flota propia (D21).** Migraciones `009` (rol `driver`, tipo de asiento `taxi_ride`, tablas `drivers`, `taxi_fares` y `taxi_settings`, y `taxi_requests` rehecha al flujo real: conductor, tarifa, cobro, marcas de tiempo y folio; dos indices unicos parciales garantizan un viaje vivo por cliente y por conductor) y `010` (`declined_by`). Nuevos `src/services/taxi.js` (ETA, cotizacion, folio y constancia), `src/services/credentials.js` (contrasena temporal, ahora compartida con empleados) y `src/routes/taxi.js` con **32 rutas**: el gerente registra conductores con contrasena temporal, los avala (`trusted`), los da de baja, carga tarifas por zona y ajusta el modulo; el conductor se marca disponible y **"ya estoy en la salida"**, ve las ofertas abiertas, acepta declarando minutos, rechaza, avisa que llego, **confirma inicio y fin** y captura el monto cobrado; el cliente consulta disponibilidad, solicita (idempotente), ve el auto y el telefono de su conductor, cancela antes del inicio y califica. El cobro en efectivo se asienta en el libro (`taxi_ride`, `cash`, pagador el cliente y beneficiario el conductor) con la nota de que el dinero paso de mano a mano; tarjeta responde 501 hasta la fase 3. Al iniciar el viaje se emite la **constancia de salida** con folio unico y vigencia, verificable **sin sesion** en `GET /api/taxi/verify/:folio` (20/min) devolviendo solo club, hora, vigencia, nombre corto y auto. Tablero y estadisticas por conductor; cierre de solicitudes que nadie tomo. `openapi.yaml`: **150 operaciones**, etiqueta *Taxi y salida segura*, 4 parametros y 17 esquemas nuevos. Eliminado `taxi-system.js`. | **399 pruebas verdes** (`npx jest`, 11 suites; 60 nuevas en `taxi.test.js`): alta solo por el gerente, contrasena temporal de 12 caracteres que no se guarda en claro y que solo abre la puerta para cambiarla, correo repetido 409 y placas repetidas 409, menor de edad 422, baja que bloquea la cuenta y reactivacion; sin aval no se puede marcar disponible; apagarse siempre limpia "en la salida"; el cliente sin conductores **no recibe un tiempo inventado** (`null`), con uno en la salida recibe 0, y con historial el promedio real (8 y 12 min -> 10); idempotencia por `client_request_id` (1 sola fila), segunda solicitud abierta 409, conductor no pide taxi 403; la oferta llega a los dos conductores, el segundo que acepta recibe **409** y el viaje no cambia de dueno, rechazar la quita solo de mi lista; no se puede saltar un paso del ciclo (409 en cada salto); iniciar emite folio `EV2-XXXX-XXXX` vigente; cerrar en efectivo crea el asiento de $150 con pagador y beneficiario correctos y devuelve al conductor a la lista, **el libro rechaza modificar y borrar ese asiento**, tarjeta 501, cortesia con monto 422, efectivo en 0 422; cancelar asignado libera al conductor y cancelar en curso 409; calificar solo terminado y una vez; la verificacion publica no expone destino, monto, telefono ni apellido, el folio no distingue mayusculas, una constancia vencida responde `valid:false` en vez de desaparecer; tablero filtrado, estadisticas (2 viajes, $300, 9 min de promedio) y cierre idempotente de solicitudes sin conductor. Cobertura `src/routes` **93.5%** sentencias. `eslint src` 0 errores. `redocly lint` valido y **150/150 rutas documentadas** (comprobado programaticamente contra el router de Express). Migracion 009 aplicada **sobre una base con datos de 001-008** sin perder filas. | rama fase2/taxi |
| 2026-09-04 | 2.7 | **Valet con QR (D22) y correccion del tablero (D23).** Migracion `011`: `valet_settings` por club (activo, punto de entrega, tarifa **0 = gratis** y terminos) y `valet_tickets` con `qr_token`, idempotencia, `requested_by`, marcas de cancelacion y el rastro de la entrega sin QR; indices unicos que impiden **dos tickets abiertos para las mismas placas** y dos autos en un cajon. Nuevos `src/services/valet.js` (codigo corto, token del QR, comparacion en tiempo constante, ocupacion y estadisticas) y `src/routes/valet.js` con **20 rutas**: el valet recibe el auto (imprime `V-XXXX` + QR), ve el tablero por estado y placas, marca "auto en la puerta", **entrega escaneando el QR** y cancela; el cliente reclama su ticket con el QR, ve sus autos, pide el suyo (idempotente) y califica; el gerente carga cajones en bloque, ajusta el modulo, ve numeros por valet y es **el unico** que puede entregar sin QR, dejando motivo, tipo de identificacion y nombre (nunca el numero del documento) en `audit_log`. La propina al valet sigue por el modulo de propinas. Corregido `GET /dashboard`. `openapi.yaml`: **170 operaciones**, etiqueta *Valet*, 2 parametros y 7 esquemas nuevos, y `revenue_today` documentado con `to_staff` y `to_drivers`. Eliminado `valet-parking-system.js`. | **430 pruebas verdes** (`npx jest`, 12 suites; **31 nuevas** en `valet.test.js`, verdes a la primera): la tarifa nace en 0; carga masiva de cajones con repetidos omitidos y duplicados en la misma peticion 400; un cajon con auto no se retira ni se desactiva (409); el ticket entrega `V-XXXX` y un token de 48 hex, y **ese token no vuelve a aparecer** ni en el tablero ni en la ficha ni en la respuesta del propio escaneo; mismas placas dos veces 409, cajon ocupado 409, cajon inexistente 422, idempotencia por `client_request_id`; el cliente reclama con el QR, otro cliente no se lo lleva (409), token falso 404, y el cliente **no ve** quien estaciono su auto ni las notas del personal; pedir el auto avisa al valet y pedirlo dos veces no es error; **el token de otro ticket no abre este auto (403) y el auto sigue estacionado**; se entrega sin haber pedido (el QR es el control), no se entrega dos veces (409) y un token corto ni llega a comparar (400); el valet **no** puede saltarse el QR (403) y el gerente si, quedando el asiento en `audit_log` **sin numero de documento**; motivo corto o identificacion desconocida 400; con tarifa >0 la entrega exige metodo de pago (422), tarjeta 501 y efectivo asienta `valet` **a favor del club, con `payee_user_id` nulo**; calificacion solo del dueno, solo entregado y una vez; estadisticas que separan las entregas sin QR; y el tablero corregido devuelve 500 de venta con 300 de propina y 150 de taxi **aparte**, donde antes devolvia 950. Cobertura `src/routes` **93%**. `eslint src` 0 errores. `redocly lint` valido y **170/170 rutas documentadas** (comprobado contra el router de Express). Migracion 011 aplicada **sobre una base con datos de 001-010**: el ticket previo conserva codigo, placas y estado, y recibe su token. | rama fase2/valet |
| 2026-09-04 | 2.8 | **Registro de integraciones POS (D24). Cierra la Fase 2.** Migracion `012`: `pos_integrations` gana encendido, datos del agente (version, equipo, IP), intervalos de latido y caducidad, rotacion de llave con autor, e indice unico global sobre el hash de la llave; `pos_sync_log` gana `integration_id`, origen, detalle e idempotencia. Nuevos `src/services/pos/registry.js` (llave, hash, salud derivada) y `src/routes/pos.js` con **9 rutas**: el gerente declara la integracion y recibe la llave del agente **una sola vez**, la enciende/apaga, la reconfigura, **rota la llave**, la elimina y consulta la bitacora con resumen de 7 dias; el agente local se reporta con `X-Agent-Key` en `POST /api/pos/agent/heartbeat` (recibe que puede hacer) y registra sus corridas en `POST /api/pos/agent/sync` (idempotente). **No se guarda ninguna credencial de SQL Server** (D16/D24). `openapi.yaml`: **179 operaciones**, etiqueta *Integracion POS*, esquema de seguridad `agentKey`, 1 parametro y 8 esquemas nuevos. Eliminado `pos-integration-system.js`. | **453 pruebas verdes** (`npx jest`, 13 suites; **23 nuevas** en `pos.test.js`): se guarda solo el hash de la llave y `credentials_encrypted` queda nulo; **la llave no reaparece en ninguna consulta**; `config` con una llave desconocida (`password`) se rechaza con 400; un proveedor inventado 400; dos integraciones del mismo proveedor 409; el modo mock no genera llave; solo el gerente entra y el de otro club recibe 403/404; **no se enciende un agente sin llave (422)**; rotar deja al agente anterior en 401 y al nuevo en 200, limpiando version y ultimo latido; una integracion encendida no se elimina (409); llave ausente, invalida **o una sesion de usuario** no entran (401); el latido guarda version y equipo y devuelve `push_orders:false`; apagar desde la app llega al agente en el siguiente latido; el agente puede reportarse fallando y el error se limpia al recuperarse; un agente callado se reporta **stale**, no sano; una corrida queda en la bitacora con su resumen, una fallida pone la integracion en rojo, el reintento con la misma clave no duplica, con la integracion apagada no escribe nada (403) y un tipo inventado 400; el webhook del POS sigue en 501 y el cliente de SoftRestaurant no finge funcionar. `eslint src` 0 errores. `redocly lint` valido y **179/179 rutas documentadas**. Migracion 012 aplicada **sobre una base con datos de 001-011** conservando la integracion y la corrida previas. | rama fase2/pos |
| 2026-09-04 | 3.1 | **WebSocket autenticado y sin estado propio (D25).** `src/ws.js` reescrito por completo y nuevo `src/realtime/hub.js` (registro de conexiones por club y por usuario, entrega filtrada por audiencia). El servidor anterior guardaba **cinco mesas fijas y un muro de actividad en memoria** y aceptaba `take_seat`, `send_drink` y `send_flirt` de cualquier socket anonimo; todo eso se elimino. Ahora: autenticacion con el mismo access token de la API, preferentemente en el subprotocolo `bearer, <token>` y con la query como alternativa; el usuario se **carga de Postgres**, no del token; el socket es de solo lectura (solo `ping`) y cualquier otro mensaje responde `unsupported` remitiendo a la API REST; eventos dirigidos por audiencia (club, roles o personas) que nunca cruzan de club; ids de evento como cadena (64 bits); cierre con codigos propios (`4401` sin token, `4001` token vencido, `4002` sesion reemplazada, `4403` cuenta bloqueada, `4004` cliente abusivo, `1001` reinicio); latido con corte de sockets muertos y **cierre al expirar el token**; limite de 120 mensajes/min y 4 KB por trama; apagado limpio con SIGTERM; `/health` con conteos y **sin datos de nadie**. `deliver()` queda como unico punto de entrada de eventos, listo para Redis en 3.2. Contrato del canal documentado en `docs/api/websocket.md`. | **467 pruebas verdes** (14 suites; **14 nuevas** en `ws.test.js`, contra un servidor real en un puerto efimero): conecta por subprotocolo y por query; el extractor **prefiere el subprotocolo** cuando llegan los dos; sin token, con uno invalido y con uno vencido cierra con codigos **distintos** (lo que le dice al cliente si debe refrescar o volver a entrar); una cuenta bloqueada entre la emision del token y la conexion recibe `4403`; contrasena temporal pendiente tambien; `ping`/`pong` funciona y `take_seat` responde `unsupported`; un mensaje que no es JSON **no tumba la conexion**; un evento sin audiencia llega a los dos usuarios del club y **cero** al de otro club; una audiencia por rol llega solo al cantinero y la clienta sigue recibiendo lo suyo; una audiencia por persona no se filtra al gerente; un id de 64 bits viaja intacto como cadena; el registro no tiene `tables` ni `activity` y suelta las conexiones al cerrarse; `/health` no expone el id de nadie; y **una sexta conexion desplaza a la mas vieja con `4002`** en vez de rechazar al recien llegado. `eslint src` 0 errores. | rama fase3/ws |
| 2026-09-04 | 3.2 | **Redis pub/sub: del COMMIT al socket (D26).** Migracion `013`: disparador `events_notify` con **`pg_notify` (transaccional: se entrega solo al confirmar y se descarta con el ROLLBACK)** y tabla `realtime_relay_state` con el cursor. Nuevos `src/realtime/relay.js` (buzon de salida: eleccion de lider con candado consultivo de Postgres, `LISTEN`, barrido desde cursor persistido, ventana de 5 min para no entregar en vivo lo viejo, publicacion a `ev2:events`) y `src/realtime/subscriber.js` (suscripcion dedicada; un mensaje corrupto se cuenta y se descarta). Enganchados en `src/index.js` (la API releva; `/health` informa lider y cursor) y `src/ws.js` (el socket suscribe; `/health` responde **503 `degraded`** si la suscripcion esta muerta, porque un servidor de sockets sano que no entrega nada se ve igual de bien). CI: servicio Redis anadido. | **480 pruebas verdes** (15 suites; **13 nuevas** en `realtime.test.js`, contra Postgres y Redis reales): lo escrito dentro de una transaccion **no sale hasta el COMMIT** y lo deshecho con ROLLBACK **no sale nunca**; 10 eventos llegan en orden, sin repetir y sin extras; el relevo **recupera lo escrito mientras estuvo caido**; una instalacion nueva **no reproduce la noche entera**; los eventos viejos no se entregan en vivo; **solo un proceso releva** (con dos, ese evento habria salido dos veces) y la reserva **toma el mando cuando el lider se va**; de la API al socket con la audiencia intacta despues de pasar por Redis; un evento de otro club no cruza; `/health` avisa cuando la suscripcion se cae; y dos mensajes corruptos en el canal no impiden que el siguiente evento llegue. **Tres defectos reales encontrados y corregidos durante las pruebas:** (1) `SELECT id::text AS id ... ORDER BY id` hacia que Postgres ordenara por el **alias de texto** y los eventos salieran 1, 10, 2 — orden alfabetico; se califico `ORDER BY e.id` y se renombro el alias; (2) un relevo detenido seguia barriendo y publicaba eventos ya publicados por su sucesor: `stop()` ahora espera el barrido en vuelo y el bucle comprueba el liderazgo **fila por fila**; (3) los barridos se serializaban con una bandera que permitia solaparse bajo rafagas (tres a la vez) — ahora se encadenan con promesas, imposible por construccion. Ademas se corrigio una fuga de listeners en la conexion del relevo. `eslint src` 0 errores. | rama fase3/redis |
| 2026-09-04 | 3.3 | **Reconexion con `lastEventId` (D27).** El socket acepta `?since_id=` y, antes de entregar nada en vivo, envia lo que se perdio: `resume_started` -> los eventos en orden -> `resume_complete`. La recuperacion **aplica la misma filtrada por audiencia que la entrega en vivo** y nunca cruza de club. Los eventos en vivo que llegan durante la recuperacion **se retienen y se entregan al final**, descartando los que la recuperacion ya envio (`pendingAfter()` en `hub.js`, funcion pura). Limites explicitos: **500 eventos y 24 horas**; si el hueco es mayor, `complete: false` y `resync_required` con `reason: gap_too_large` para que el cliente recargue desde la API en vez de creerse al dia con una historia a medias. Un `since_id` que no es entero responde `bad_since_id` y la conexion sigue en vivo. La recuperacion se envia **en trozos de 50 cediendo el bucle de eventos**, para que un telefono poniendose al dia no deje sin atender al resto de la sala. `docs/api/websocket.md` actualizado con el protocolo completo. | **491 pruebas verdes** (16 suites; **11 nuevas** en `resume.test.js`): el `last_event_id` del welcome sirve como punto de partida y trae los 3 eventos perdidos en orden; sin `since_id` no se reproduce nada; un `since_id` al dia devuelve 0 sin inventar un error; uno del futuro no rompe; uno invalido avisa y la conexion sigue entregando; **de tres eventos con audiencias distintas solo se reproduce el que era para esa persona**; lo de otro club no se reproduce ni pidiendolo (y el de aquel club si ve los suyos, prueba de que existen); lo mas viejo que la retencion no se reproduce; **un evento en vivo durante la recuperacion se entrega despues, no en medio** (verificado tambien por posicion relativa a `resume_complete`); el descarte de duplicados se prueba como funcion pura con enteros de 64 bits; y un hueco de 501 eventos entrega 500, marca `complete: false` y pide recargar. Estable en 5 corridas seguidas. `eslint src` 0 errores. | rama fase3/resume |
| 2026-09-05 | 3.6 | **Pagos manuales (D28).** Migracion `014`: `manual_payment_options` (donde paga el cliente: efectivo, Zelle, Cash App, transferencia, SPEI, con destino e instrucciones) y `manual_payments` (la declaracion y su revision), con **cuatro indices unicos parciales**: una declaracion abierta por asiento, **un solo pago confirmado por asiento**, idempotencia por `client_request_id` y **una referencia no puede pagar dos cosas**. Nuevos `src/services/payments.js` (liquidacion compartida y efectos sobre la reservacion) y `src/routes/payments.js` con **12 rutas**: el gerente carga y retira formas de pago y revisa la cola con su resumen; el cliente ve a donde pagar, declara (idempotente), consulta y retira su declaracion; el gerente **confirma** (unico paso que mueve el libro a `paid`) o **rechaza con motivo**; y la anfitriona **registra el efectivo recibido en la puerta**, declarado y confirmado en un paso. Confirmar el deposito **pasa la reservacion a `confirmed`** en la misma transaccion SQL. Se anadio a `CLAUDE.md` una seccion con las tres trampas de SQL que ya nos costaron tiempo. | **517 pruebas verdes** (17 suites; **26 nuevas** en `payments.test.js`): una transferencia sin destino no es forma de pago (422) y el efectivo no pide folio; **declarar no cobra nada** (el asiento queda `pending_manual`, no `paid`); no se declara el cobro de otro (404); un cobro ya pagado o cancelado no admite pago (409); dos declaraciones abiertas para el mismo cobro 409; **la misma referencia en dos cobros 409** (probado tambien en minusculas); idempotencia; retirar la declaracion devuelve el cobro a `pending` y permite volver a declarar; **confirmar es lo unico que mueve el libro a `paid`** con `confirmed_by` del gerente; **confirmar el deposito confirma la reservacion** y emite los dos eventos; un monto distinto responde 422 **con ambas cifras** y no toca el libro; **confirmar dos veces no acredita dos veces**; rechazar devuelve a `pending`, avisa al cliente con el motivo y permite reintentar; ni el cliente ni la anfitriona confirman; el efectivo registrado por la anfitriona queda `paid` con proveedor `cash` y **constancia de quien lo tomo y de parte de quien**; una transferencia registrada por el personal sigue exigiendo folio; y el gerente de otro club no ve ni confirma nada de aqui. Cuarto `42P08` de la serie, corregido y **documentado en `CLAUDE.md` para que no vuelva**. `eslint src` 0 errores; `redocly lint` valido; **191/191 rutas documentadas**. Migracion 014 aplicada sobre una base con datos de 001-013: el asiento previo intacto y el libro sigue rechazando cambios. | rama fase3/pagos-manuales |
| 2026-09-05 | 3.4/3.5 (preparacion) | **Hueco listo para las pasarelas (D29).** Nuevo `src/config/payments.js`: lee las llaves de Stripe y Mercado Pago de **variables de entorno** (nunca de la base de datos), deduce el modo por el prefijo (`sk_test_`/`sk_live_`, `TEST-`) y reporta **que variable falta** por cada pasarela. Nueva ruta `GET /nightclubs/:id/payment-providers`: el gerente ve la lista de pendientes, el cliente solo lo que ya sirve y su llave publica. `src/index.js` lo imprime al arrancar y **se niega a arrancar con llaves `live` fuera de produccion**. `.env.example` con los seis campos vacios y de donde sacar cada uno; `docs/PAGOS_SETUP.md` con el paso a paso para el dueno. | **522 pruebas verdes** (5 nuevas): sin llaves la respuesta enumera exactamente las tres variables que faltan y `manual_available` sigue en true; con llaves puestas reconoce modo prueba y produccion en ambas pasarelas; **la llave secreta y la del webhook no aparecen en ninguna respuesta** y la publica si, porque la usa el navegador; el cliente **no ve pasarelas a medio configurar**; y con una llave `live` fuera de produccion `assertSafeMode()` impide el arranque. `redocly lint` valido; **192/192 rutas documentadas**. | rama fase3/config-pagos |
| 2026-09-05 | 5.1 | **Cliente web compartido (D30).** Nuevos `web/js/api.js` (476 lineas) y `web/js/format.js`, sin paso de compilacion, en `window.EV2`. Sesion (login, logout, `resume()` al recargar), `apiFetch` con Bearer y `X-Request-Id`, **renovacion unica en vuelo**, reintentos **solo en GET**, errores tipados con `fieldErrors()` listos para pintar junto al input, y un cliente de tiempo real que implementa el contrato completo de `docs/api/websocket.md` (subprotocolo, `since_id`, descarte de duplicados, codigos de cierre, espera creciente con ruido). Idioma ES/EN y formato de dinero **sin convertir a float**, con suma en centavos enteros. | **547 pruebas verdes** (18 suites; **25 nuevas** en `web-client.test.js`, con `fetch` y `WebSocket` falsos): el token de acceso **no queda en el almacenamiento** y el de refresco si; con `localStorage` bloqueado (navegacion privada) la sesion sigue en memoria; cerrar sesion limpia aunque el servidor no conteste; `resume()` guarda el token rotado, no el viejo, y con uno invalido no deja sesion a medias; un 401 renueva y reintenta **una** vez y el reintento va con el token nuevo; **diez peticiones caducadas a la vez disparan UNA sola renovacion**; un GET caido por red se reintenta y **un POST no** (una sola llamada); el error trae codigo, folio y errores por campo; el socket manda el token en el subprotocolo y **no en la URL**, entrega por tipo, al reconectar pide `since_id` y **no entrega dos veces** el evento repetido, con 4001 renueva y reconecta con el token nuevo, con **4002 no reconecta**, avisa `resync_required`, y cerrar a proposito no dispara reconexion. **Dos defectos encontrados por las pruebas:** (1) la espera entre reintentos se reiniciaba con solo abrir el socket, asi que un servidor que se cae al instante recibiria reintentos cada medio segundo para siempre -ahora solo se reinicia si la conexion **se sostuvo**; (2) el primer reintento esperaba el doble de `baseMs`, corregido para que el nombre signifique lo que dice. Estable en 5 corridas. `eslint src` 0 errores. | rama fase5/api-js |
| 2026-09-05 | 5.2 | **Pantalla del cliente contra datos reales (D31).** `web/index.html` reescrita (era una cascara con un formulario falso y `screen-main` vacio) conservando la marca; nuevos `web/js/client.js` (carrito, plano, estados de pedido, eventos) y `web/js/index-screen.js` (solo une DOM con API y socket). Entrar y **crear cuenta de verdad** (18+, terminos y opt-in de flirt), plano agrupado por planta y zona con mesas que se pueden elegir, sentarse y dejar la mesa, menu con categorias y existencias, carrito con **suma en centavos**, pedido **idempotente**, y la lista de pedidos **avanzando sola por el socket** con aviso cuando esta listo. Reconexion, `resync_required` que recarga, y sesion vencida que avisa antes de recargar. | **568 pruebas verdes** (19 suites; **21 nuevas** en `web-cliente.test.js`) mas una **verificacion en navegador real** (Chromium) contra la API, Postgres y Redis levantados: la pantalla resuelve el club, entra con el usuario sembrado, **el socket queda en vivo**, pinta **las 55 mesas reales**, se sienta (CELEBRATION SUITE 51), lista **30 bebidas**, el carrito calcula $120.00 y el pedido se crea y aparece. Y la prueba que justifica todo el tiempo real: con el cantinero moviendo el pedido desde otra sesion, el telefono paso a **Confirmado -> Preparando -> ¡Listo!** sin recargar nada, con su aviso. **Dos defectos que solo el navegador encontro:** (1) se usaba `/floor-plan`, que **no dice quien esta sentado**, asi que al recargar la app nunca sabia cual era tu mesa -se cambio a `/tables`; (2) los pisos del club son `baja` y `alta`, no numeros, y ordenarlos restando daba `NaN`. Ambos con prueba propia ahora. **Limitacion del entorno:** el proxy de salida bloquea Tailwind y las fuentes, asi que **el aspecto visual no se pudo verificar aqui**; la estructura y los datos si. `eslint src` 0 errores. | rama fase5/cliente |
| 2026-09-05 | 5.2 (cierre) | **La app funciona detras del proxy, en un solo origen.** Al preparar la prueba con `docker compose` aparecio que **nginx servia los archivos pero no reenviaba `/api` ni el socket**: la pantalla de acceso se veria bien y no haria nada. `deploy/nginx-web.conf` ahora reenvia `/api/` a la API y `/ws` al servidor de tiempo real con las cabeceras de actualizacion de protocolo; el compose declara `depends_on: ws` (nginx resuelve los upstream al arrancar y sin eso el contenedor web no levanta); el cliente usa la ruta `/ws` por omision; y el slug por omision de la pagina se alineo con el que crea la semilla (`ev2`), que no coincidian. Instrucciones en el README. | **572 pruebas verdes** (4 nuevas) y verificacion con **nginx real** (1.24) delante: `index.html`, `js/api.js` y `js/client.js` 200; `/api/nightclubs/by-slug/ev2` devuelve el club a traves del proxy; `/ws` responde **101 Switching Protocols**; y el flujo completo del cliente vuelve a pasar **por el proxy, con cero errores de consola**, incluidos los eventos en vivo (Confirmado -> Preparando -> ¡Listo!). **Defecto encontrado y corregido:** un origen que no esta en `ALLOWED_ORIGINS` provocaba **`500 Internal server error`** sin decir nada; el navegador manda `Origin` incluso en peticiones al mismo origen cuando no son GET, asi que es lo primero que golpea un despliegue mal configurado. Ahora responde **403 `origin_not_allowed`** nombrando el origen y la variable a corregir, con prueba propia. | rama fase5/compose-proxy |

## Próximo paso

**El inventario ya es real y por barra.** Lo que sigue, en orden:

1. **Confirmar 26 tamaños de botella** con el dueño (Clase Azul, Don Julio 1942, Blue Label, Dom Pérignon, los 30-30…). Se cargaron con 750 ml y la pantalla de almacén los marca; hasta que se confirmen, el conteo de esas botellas no es de fiar.
2. **Primer conteo físico** en el almacén y las dos barras, desde `almacen.html`. Es lo que pone en marcha el inventario: hasta entonces todo está en cero a propósito.
3. **3.4 y 3.5 (Mercado Pago)**, en cuanto Erick tenga las cuentas: seis valores en `.env` siguiendo `docs/PAGOS_SETUP.md`. Al hacerlo, subir `mercadopago` de 1.x a 3.x y cerrar el último CVE moderado (`uuid`).
4. **QR individual por invitado** con revocación y reasignación, compartir por WhatsApp, INE antes del escaneo y pase de contingencia por folio/nombre/teléfono. Hoy el pase es por reservación, no por persona.

**Cerrado en esta sesión:** el paso 5.7 completo (barra y mesero, incluida la venta directa en barra) y la pestaña de Inventario del gerente.

**FASE 2 COMPLETA** (2.1 a 2.8). **Paso 3.3 completo. El tiempo real esta terminado** (3.1 a 3.3).

**Paso 3.6 completo** (adelantado por no depender de cuentas externas). **El club ya puede cobrar de verdad: en efectivo en la puerta y por transferencia, con revision del gerente.**

**3.4 y 3.5 quedan en pausa**, con el hueco listo: cuando Erick tenga las cuentas, pega seis valores en `.env` siguiendo `docs/PAGOS_SETUP.md` y se retoma. 3.7 y 3.8 dependen de ellas.

**Pasos 5.2, 5.4 y 5.6 completos.** Siguiente: **5.7 — pantalla de la barra** (`bartender.html`), que es la otra mitad del mismo flujo y lo que hace falta para probar una noche completa; luego 5.3 reservacion y 5.5 portal de empleados.

**Paso 5.4:** falta correr `eslint` + `jest` en la PC del desarrollador (este entorno no tiene Node) y hacer el `git push` de la rama `fase5/propinas`.
| 2026-09-06 | 5.2 (flirt) | Pantalla "Conecta" del cliente y bandeja de moderacion del gerente: `web/js/flirt.js` (decisiones) y `web/js/flirt-screen.js` (DOM); pestana nueva en `index.html` con los tres interruptores de privacidad y la lista de bloqueados en el perfil; helpers de moderacion en `web/js/manager.js` y pestana Reportes en `manager.html`/`manager-screen.js`; textos es/en. El service worker precacheaba 11 archivos de menos (propinas, musica, reservacion, personal, pagos, turno, puerta): corregido y version subida a ev2-v2. Borradas las 4 maquetas viejas sin API (`table-selector`, `tip-dancers-system`, `pricing-dance-floor-flirt`, `safe-departure`, 2 869 lineas). `jsdom` fijado en ^24: la 30 es ESM y rompe jest 29. | `web-flirt.test.js` 52 pruebas (decisiones) y `web-flirt-dom.test.js` 13 (la pantalla real en jsdom: la puerta de consentimiento, el aviso de cobro del trago, el globito, reportar). 169 pruebas web verdes en total; eslint sin errores; cada id que usa el controlador existe en el HTML. | (pendiente de push) |
| 2026-09-06 | 5.6 (cierre) | Salida segura completa. La constancia se enseñaba como un folio de seis caracteres sin nada alrededor, y debajo prometia una pagina de verificacion que no existia. Ahora: ficha completa en la pantalla del cliente (vigente/vencida, folio, pasajero, conductor, placa, color y modelo, emision, vencimiento y descargo) desde `GET .../certificate`; `web/verificar.html` publica, sin cuenta, que acepta el folio por URL para un QR (`web/js/verify-screen.js`); codigo de conducta que el cliente lee y ACEPTA antes de pedir (migracion 015 `conduct_accepted_at`, 422 si no acepta, hora guardada y visible para el gerente); el gerente edita el texto y la vigencia de la constancia; contactos de emergencia a la vista, marcables. Decisiones nuevas en `web/js/taxi-ride.js`. | `web-conduct-certificate.test.js` 16 pruebas y `web-verify-dom.test.js` 12 (la pagina real en jsdom: vencida no se enseña como vigente, folio inexistente, limite de intentos, QR). 6 pruebas de servidor nuevas en `taxi.test.js`. 233 pruebas web verdes; eslint limpio; openapi valido. | (pendiente de push) |
| 2026-09-07 | 5.4 | **Pantalla de propinas del cliente rehecha segun el mockup `docs/Propinas`** (movido a `docs/referencia/propinas-mockup.html`). La pestana "Show" (`#view-show` en `index.html`) pasa de dos botones fijos (Personal/Musica) a una tira de pestanas **generada de quien esta en turno de verdad** (`staff/on-shift`): una por rol con gente, mas **Musica** y **Reconocimiento**. Las tarjetas del mockup traian nombres, estrellas de calificacion y totales inventados: **se quitan**; quedan nombre real, zona y minimo del rol, y la hoja de propina que ya existia. La pestana Musica gana selector de DJ (solo con 2+ en turno) y dedicatoria; la API ya aceptaba `dj_user_id` y `message`. Nueva pestana **Reconocimiento** contra `GET /leaderboard` real, con toggle noche/mes y **sin montos** para el cliente (decidido en 2.5): puesto, nombre, rol y cuanta gente lo reconocio. Helpers puros nuevos en `web/js/tipping.js` (`roleTabs`, `boardRows`). Sin cambios de backend ni migraciones; `web/sw.js` no cambia (no hay archivos nuevos). Claves i18n es/en anadidas. | **Pendiente de correr en la PC del desarrollador** (este entorno no tiene Node): `eslint`, `npx jest web-tipping-songs-booking web-i18n web-show-dom` y la regresion completa. Pruebas escritas: 11 casos nuevos de helpers en `web-tipping-songs-booking.test.js` (`roleTabs`, `boardRows`) y **`web-show-dom.test.js` nuevo** (14 casos en jsdom sobre el `index.html` real: las pestanas salen del turno, cambiar de pestana cambia el panel, sin estrellas ni totales falsos, el reconocimiento sin `$`, el toggle mes vuelve a pedir, la dedicatoria y el DJ solo viajan en cancion nueva y recortada a 280, la hoja de propina manda el importe tocado, un evento del socket recarga y uno ajeno no). | rama fase5/propinas |
| 2026-09-09 | 2.2 (cierre) | La carta REAL del club, exportada de SoftRestaurant11 y entregada por el dueño. `server/seeds/data/ev2-menu.json` + `npm run seed:menu`: 129 productos de barra (Drinks 52, Botellas 48, Shots 13, Sin alcohol 9, Cervezas 7), las 48 botellas reales como productos de reservación y 3 extras (pulsera VIP y los dos paquetes de cumpleaños). Se descartaron las copias B2/B3 (el mismo catálogo para las otras dos barras), los 122 SUSPENDIDOS, lo bloqueado en la caja, lo de precio abierto y lo de precio cero. La llave es `productos.idproducto`, la misma de la futura sincronización. Se quitaron las 6 botellas y 8 extras INVENTADOS de la lista de precios y las 30 bebidas inventadas del seed de desarrollo. Botones de cover real ($150/$100/$50) en el formulario de la noche. `.gitattributes` nuevo: un editor de Windows guardó 8 archivos en CRLF y git los vio enteros como modificados. | `menu-data.test.js` 16 pruebas sobre el archivo (sin base): precios >0 y de 2 decimales, sin ids ni nombres repetidos, sin copias B2/B3, botellas de reservación idénticas a las de la carta, ningún código inventado. `events.test.js` y `reservations.test.js` actualizadas a códigos reales. 228 pruebas verdes; eslint limpio. | (pendiente de push) |
| 2026-09-10 | 5.2 (carta) | Imagen por producto en el menu del cliente. `web/js/drink-art.js`: 18 ilustraciones dibujadas en SVG (copa, vaso alto, huracan, vaso bajo, michelada, cubeta, caballito, ronda, cinco botellas, agua, jugo, refresco, energetica, paquete) y la regla que le asigna una a cada producto por su nombre, no por su grupo: `CUB. TECATE` es cubeta y `TECATE` es botella; `EN LAS ROCAS BLACK LABEL` es vaso bajo; un blanco se pinta transparente y un añejo ambar. Se dibujan y no se descargan: pesan poco, funcionan sin señal y no fingen ser una foto del producto. **Si el producto tiene `image_url`, la foto gana** — la columna existia en los 129 y ninguna pantalla la usaba. | `web-drink-art.test.js` 22 pruebas: los 129 productos reales reciben dibujo, ninguno devuelve vacio, un producto a medias tampoco se queda sin dibujo, la foto gana y una foto en blanco no cuenta. Revisado a ojo el render de la carta real. 111 pruebas de pantalla verdes (incluidas las de la pantalla de propinas); eslint limpio. | (pendiente de push) |
| 2026-09-10 | 5.3 (puerta, servidor) | Sentarse deja de ser algo que el cliente hace solo: se entra por la puerta. Migracion 016 (`pass_code` por reservacion, `checked_in_at/by`, tabla `door_admissions`, el libro aprende el tipo `cover`). `services/door.js` (codigo del pase, lectura de QR o tecleado, cuando abre) y `services/seating.js` (sentar/levantar en un solo lugar). `routes/door.js`: escanear el pase, mirar sin gastarlo, vender acceso general o extra VIP, y el aforo. `tables.js`: sentar y levantar pasan a ser solo del personal. **Defecto de fondo corregido:** marcar una reservacion como `seated` pintaba la mesa de ocupada pero NO metia a nadie en `table_occupants`, asi que el cliente recibido por la hostess seguia sin poder pedir. | `door-pass.test.js` 19 pruebas puras (alfabeto sin letras que se confunden al dictarlas, sin repetidos, cuando abre un pase) y `door.test.js` 22 de punta a punta (sienta de verdad, el mismo QR no abre dos veces, el pase de otro club no abre, la cortesia no inventa un cobro, el aforo separa VIP de general). eslint limpio; openapi valido con las 4 rutas nuevas. | (pendiente de push) |
| 2026-09-10 | 5.3 (puerta, pantallas) | El pase en el telefono del cliente (QR + codigo escrito, guardado en el telefono para que abra sin señal) y el escaner en la pantalla de Puerta: camara cuando el telefono la soporta y campo para teclear SIEMPRE a la vista, porque Safari no lee QR dentro de una pagina. Venta de acceso general y de extras VIP con los covers reales de un toque, y aforo en vivo. En la pantalla del cliente **desaparecieron los botones de sentarse y de dejar la mesa**: el plano quedo para mirar. `web/js/door-scan.js` nuevo; `qrcode` como dependencia (el QR lo dibuja el servidor: un generador en el navegador serian 200 lineas de Reed-Solomon que nadie revisa, y un QR mal generado no falla, simplemente no lee). | `web-door-scan.test.js` 15 pruebas (lo que se arregla hablando es ambar y no rojo, un resultado desconocido NO se pinta de verde, el total en centavos enteros, un extra sin pase leido no se vende). 541 pruebas verdes en el equipo y 111 de pantalla en jsdom; eslint limpio. | (pendiente de push) |
| 2026-09-11 | 5.7 (cobro en la mesa) | **Cada pedido nace con su cobro y la barra no sirve a credito.** Migracion 017 (`drink_orders.taken_by`, y la terminal del club como forma de pago). `services/orders.js` asienta la transaccion junto con el pedido; la transicion a `confirmed` se niega mientras no este `paid`; pagar el cobro confirma el pedido en la misma transaccion SQL, asi que no hay un segundo boton que alguien olvide. **El mesero levanta el pedido de la mesa que atiende** (`on_behalf_of` verificado contra `table_occupants`: un id mal tecleado le cargaba el trago a alguien del otro lado del salon); sin cliente identificado el cobro queda a su nombre, que es quien recibio el dinero. `/manual-payments/register` solo acepta lo que ya esta cobrado -efectivo o terminal con folio-; una transferencia sigue pasando por el gerente. Cancelar un pedido pagado **no borra el dinero**: queda el reembolso pendiente. En las pantallas: hoja de pedido a pantalla completa desde cada mesa (todas las mesas, no solo las que tienen gente registrada), lista de "por cobrar" con contador en la pestaña, y el cliente ve que su pedido esta esperando pago. **Tres defectos previos corregidos:** (1) la migracion 016 apuntaba `door_admissions.event_id` a `events(id)` -el buzon de salida, con id BIGSERIAL- en vez de `events_calendar(id)`, **era inaplicable y habria reventado en el primer despliegue al VPS**; (2) flirts y tragos al personal insertaban su propio asiento junto al que ahora crea el pedido, asi que **un trago invitado se cobraba dos veces**; (3) `/tables/:id/release` sin `user_id` significaba "el mesero se levanta a si mismo". | **611 pruebas de servidor** contra Postgres 16 y Redis reales (21 nuevas de cobro y de pedido levantado por el mesero) y **607 de pantalla** (35 nuevas: 26 del modulo `order-taking.js` y 9 de la hoja contra el `staff.html` real, que comprueban que cada id que busca el controlador existe y que no queda un texto sin traducir). eslint limpio; openapi valido. **Nota:** las 12 pruebas de `tables.test.js`, 4 de `flirt.test.js`, 1 de `catalog.test.js` y 3 de `floorplan.test.js` **llevaban rotas desde el paso de la puerta** y nadie lo sabia porque este equipo no tiene Postgres; se corrigieron aqui. | commits `634dde6` y `68d819f` (pendientes de push) |

| 2026-09-14 | 2.x + 5.7 (inventario real) | **La existencia dejo de ser un numero inventado.** Migracion `018` reescrita antes de aplicarse en ningun lado: la existencia vive en el INSUMO y EN UN LUGAR (`supply_locations`: almacen, barra baja, barra alta; `supply_stock` por par insumo-lugar), la receta en `drink_supplies`, el kardex en `supply_movements` (solo inserciones, con lugar, motivo, responsable, autorizacion y los dos lados de un traspaso unidos por `transfer_group`), `zone_bars` dice que barra atiende cada zona, `delivery_points` da QR a cada mesa y a la pista, rol nuevo `warehouse`, y la tabla `inventory` vieja se elimina (sus numeros venian de la semilla: 12 o 100 por producto). Nuevo `scripts/import-sr11-catalog.py` convierte los dos export del dueno (`productos.xls`, `recetas.xls`) en tres archivos de semilla: **129 productos** con el precio unificado al mayor entre barras, **88 insumos** con su presentacion, y **129 recetas** con la onza de la barra (**1 oz = 30 ml**). `services/inventory.js` y `routes/inventory.js` reescritos por lugar (recepcion, traspaso, conteo, merma, cortesia, salida, correccion, kardex, recetas, minimos por estante); el pedido resuelve su barra por la zona de la mesa y descuenta de ese estante; la cola de la barra se filtra por barra, se ordena por hora de PAGO y se puede reacomodar sin tocar la auditoria (`bar_position`); nueva pantalla `web/almacen.html` + `js/warehouse.js` + `js/warehouse-screen.js`; el mesero levanta pedidos en la pista y confirma entregas; `openapi.yaml` +23 operaciones (219). | **1,336 pruebas verdes** (43 suites; 83 nuevas). Carga real verificada contra Postgres 16: 3 lugares, 8 zonas asignadas, 88 insumos, 129 recetas (270 renglones), 60 puntos de entrega, **existencia cero**, y 26 presentaciones marcadas POR CONFIRMAR. Prueba de extremo a extremo con la API arriba: se reciben 12 botellas de Buchanan's 12 a $900 (costo $1.20/ml), se surten 3 a la barra de abajo y 2 a la de arriba, el mesero levanta 6 tragos en una mesa de planta baja y **bajan 180 ml SOLO del estante de abajo** (4,500 -> 4,320) mientras el de arriba queda intacto; el cobro en efectivo confirma el pedido y aparece en la cola de su barra, la de arriba con cero. El kardex explica cada saldo: recepcion, los dos lados de cada traspaso y el consumo. `eslint src` 0 errores; `redocly lint` valido. | rama fase5/inventario-por-barra (pendiente de commit: la PC se desconecto a media sesion) |

| 2026-09-14 | Verificacion (seguridad y funcionalidad) | **Suite de seguridad nueva** (`server/tests/security.test.js`, 52 pruebas) que intenta hacer lo que el sistema NO debe permitir: entrar sin token, con token de otro secreto, vencido, con el rol editado en el payload o con `alg: none`; cada rol haciendo lo que no le toca (cantinero corrigiendo saldos, mesero recibiendo mercancia, almacen reescribiendo recetas, cliente leyendo el kardex); el club vecino leyendo o moviendo este inventario por las 20 rutas nuevas; escribir un saldo por la puerta de atras (`stock` en el cuerpo, estante en negativo, dos peticiones simultaneas); inyeccion SQL por `search`, `category` y `kind`; y fugas en la respuesta (token de QR, correos, SQL, stack traces). Tambien una **prueba de funcionalidad de extremo a extremo** re-ejecutable (`docs/api/smoke-test-inventario.sh`, 33 comprobaciones) que recorre la noche completa contra la API viva: entrada de mercancia, surtido a las dos barras, registro de cliente (y rechazo de un menor), sentarlo, pedido del mesero, cobro en efectivo, la barra correcta preparando y la otra sin verlo, entrega, y el conteo fisico revelando la merma. **`npm audit fix`** aplicado: express 4.22.2 -> 4.22.3 con `qs` y `body-parser` parcheados (2 CVE moderados cerrados); queda 1 pendiente que exige subir `mercadopago` de 1.x a 3.x (cambio mayor, se hara al escribir 3.5). | **1,388 pruebas verdes** (44 suites). Las 52 de seguridad pasan todas; la primera corrida encontro un defecto **de la propia prueba** (un motivo de 2 caracteres chocaba con la validacion antes de llegar al chequeo de club), no del sistema. Smoke de extremo a extremo: **33/33**, y vuelve a pasar corriendolo dos veces seguidas sobre la misma base (los saldos se comparan por diferencia). Contra el servidor en vivo: rate limit de `/auth/login` corta al intento 11 (`429`), un origen fuera de `ALLOWED_ORIGINS` recibe `403 origin_not_allowed`, y las cabeceras de helmet estan puestas (CSP, HSTS, `nosniff`, `X-Frame-Options`, sin `x-powered-by`). Postgres y Redis **sin `ports`** en los dos compose. `eslint src` 0 errores; `redocly lint` valido. | rama fase5/inventario-por-barra |

| 2026-09-14 | 5.7 (cierre) + inventario del gerente | **Las dos mitades que faltaban.** (1) **Venta directa en la barra**: el cliente que llega, pide y paga ahi mismo -- la mitad de la clientela de una barra, que hasta ahora el sistema no contemplaba (el pedido exigia una mesa, asi que el cantinero servia el trago y el inventario nunca se enteraba). Hoja nueva en `bartender.html` con la carta de ESA barra (solo lo que alcanza en ese estante), carrito que respeta existencias, efectivo o terminal con folio obligatorio, y el pedido cae en su propia cola ya pagado. El backend no necesito cambios: `bar_location_id` sin mesa ya funcionaba. (2) **Pestaña de Inventario en `manager.html`**: valor del inventario al costo por lugar, bajo minimo, presentaciones sin confirmar, existencias por estante, **kardex** de solo lectura, minimos por estante, y sobre todo el **editor de recetas con margen** -- que hasta ahora solo existia como ruta de la API, o sea que corregir una receta requeria un `curl`. Nuevas decisiones en `web/js/manager.js` (`recipeMargin`, `sortRecipes`, `validateRecipe`, `recipePayload`) y en `web/js/order-taking.js` (`barSaleBlocker`, `sellableDrinks`). | **1,443 pruebas verdes** (46 suites; **55 nuevas**): 9 de la venta en barra en el servidor (sin mesa, a nombre del cantinero, descuenta de ESA barra, 409 si la barra no tiene, idempotencia, y **el cliente NO puede elegir de que barra sale su pedido**), 21 de decisiones (margen en centavos enteros, orden por margen, receta vacia valida, insumo repetido) y 23 de DOM en jsdom (`web-bartender-dom` y `web-manager-dom`, que ademas comprueban que **cada pestaña con boton tenga panel y este declarada en `TABS`**). Verificacion en **Chromium real** contra la API, Postgres y Redis levantados: **15/15** en la venta en barra (elige barra, filtra la carta, "quedan 196" de ese estante, el boton nace apagado, la terminal exige folio, cobra $300 y la venta aparece en la cola sin el aviso de "sin pagar", cero errores de consola propios) y **25/25** en la pestaña del gerente (valor $40,617.60 al costo, 26 presentaciones sin confirmar, 88 insumos con su desglose por estante, 129 recetas, margen 76% de un trago, quitar y volver a poner un ingrediente, guardar, y **releer la receta para confirmar que el cambio quedo**). **Dos defectos que solo el navegador encontro:** (1) la pestaña pintaba **`MX$0.00` y ceros mientras cargaba** -- que no es "cargando", es un dato falso, y el gerente que lo alcanza a leer se lleva la idea de que la bodega esta vacia: ahora dice "Cargando el inventario" hasta que llega la respuesta; (2) la busqueda se **arrastraba entre vistas** ("BUCHANANS" busca un producto en recetas y un insumo en existencias), dejando la lista vacia sin que se viera por que: ahora se limpia al cambiar de vista. `eslint src` 0 errores; `redocly lint` valido. **Limitacion del entorno:** el proxy de salida bloquea los CDN (Tailwind, Font Awesome, fuentes), asi que el **aspecto visual** no se pudo verificar aqui; la estructura, los datos y el flujo si. | rama fase5/inventario-por-barra |

| 2026-09-14 | 5.x (pase individual por invitado) | **Ocho personas dejaron de entrar con un solo codigo.** Migracion `019`: `guest_passes` (uno por persona, firmado, de un uso, revocable y reasignable, con `replaces_id` para seguir la cadena de reasignaciones), `door_id_checks` (la revision de identificacion, que se gasta con el escaneo) y `guest_pass_events` (auditoria **solo inserciones** con disparador). La migracion emite los pases de las reservaciones que ya existian y **el titular conserva su codigo**, para que un QR ya repartido siga abriendo. Servicio nuevo `services/guest-passes.js`: firma HMAC derivada de `JWT_SECRET` (sin variable nueva), lectura del payload del QR, del enlace de WhatsApp y del codigo tecleado, y la decision de si un pase abre. Rutas nuevas `routes/passes.js` (listar, ver con QR, poner nombre, **compartir**, revocar, reasignar, historial, y el **enlace publico sin sesion** que abre el invitado) y en `routes/door.js` (`id-checks`, `lookup`, `passes/contingency`, y `check-in` reescrito para gastar **un** pase exigiendo la revision de identificacion). Pantallas: `web/pase.html` + `js/pass-screen.js` (la pagina que abre el invitado desde WhatsApp, sin cuenta, que guarda el QR como imagen), el bloque de pases en la hoja del pase de `index.html` (repartir, renombrar, revocar, reasignar) y el **paso 1 / paso 2** en la puerta de `staff.html`: el campo del codigo **nace deshabilitado** y solo lo abre una revision de identificacion viva, con cuenta atras. `openapi.yaml` +11 operaciones (230). | **1,554 pruebas verdes** (48 suites; **111 nuevas**). De seguridad: firma falsificada rechazada sin tocar la base, pase reusado, revocado, vencido, de otro club, escaneo sin revision, revision de otro guardia, revision vieja, revision gastada, menor de edad, y que el **QR y el enlace publico no expongan un solo dato** (se comprueba contra el nombre, el correo, el id del usuario y el id de la reservacion). De integridad: la auditoria no se puede editar ni borrar, un revocado exige motivo en la base y un usado exige hora. De DOM en jsdom: cada id que buscan `pass-screen.js`, `staff-screen.js` y **`booking-screen.js`** existe en su HTML — esa ultima ataba dos archivos que **no tenian ninguna prueba entre ellos**. **Un defecto encontrado por las pruebas:** `check-in` pasaba la fila entera del pase como si fuera la reservacion, asi que leia `status` del PASE (`active`) en vez del de la reservacion y **dejaba entrar a una mesa sin pagar**; lo cazo la prueba de "sin pagar se distingue de cancelada". **Y un defecto de despliegue, encontrado revisando el VPS:** `TRUST_PROXY_HOPS` valia 1 con **dos** saltos delante (Caddy -> nginx -> API), asi que Express se quedaba con la IP de Caddy y **todas** las peticiones parecian venir de la misma: diez contrasenas equivocadas de cualquiera dejaban a todo el club sin poder entrar durante un minuto. Corregido a 2. `eslint src` 0 errores; `redocly lint` valido. | rama fase5/inventario-por-barra |

| 2026-09-14 | Verificacion del pase + dos defectos de despliegue | **Prueba de extremo a extremo de la entrada** (`docs/api/smoke-test-puerta.sh`, **59 comprobaciones**) contra la API viva: reservar para 4 emite 4 pases, compartir devuelve el enlace de WhatsApp, el invitado abre ese enlace **sin cuenta** y ve su QR, el codigo pelon y la firma cambiada dan 404, escanear sin revisar la identificacion se niega, un menor queda registrado como rechazo **sin quemar el pase**, entra UNA persona y el mismo QR ya no abre, los otros tres siguen sirviendo, un QR fabricado se rechaza, reasignar mata el codigo viejo y el nuevo abre, la contingencia vale una vez, y la auditoria explica la noche. Se puede correr dos veces seguidas: crea su propio cliente y su propia noche, y **restaura la regla de anticipo** que baja para poder reservar (comprobando que quedo restaurada y avisando fuerte si no). **Script de diagnostico** (`docs/api/diagnostico-vps.sh`): recorre los siete saltos entre el navegador y la base y se detiene a explicar el primero que falla, porque "Sin conexion. Revisa tu senal" es verdad y no dice cual de los siete se rompio. **Dos defectos de despliegue corregidos:** (1) el chequeo de salud vivia solo en `/health` y el proxy solo reenvia `/api/`, asi que **desde fuera del servidor no habia manera de preguntarle a la API si estaba viva** -- la vigilancia que pide el plan no tenia a donde apuntar y cada `curl https://dominio/api/health` contestaba 404 mandando a buscar el problema donde no estaba; ahora responde en las dos rutas. (2) `TRUST_PROXY_HOPS` valia 1 con **dos** saltos delante. Y el mensaje de fallo de red decia solo "revisa tu senal", que manda a mirar el wifi cuando el problema puede estar del otro lado: ahora dice "no se pudo contactar al servidor; si tienes internet, el servidor no esta respondiendo". | **1,559 pruebas verdes** (49 suites; 5 nuevas del chequeo de salud, que es lo que impide que se vuelva a caer uno de los dos caminos). Smoke de la puerta: **59/59**, dos veces seguidas. En **Chromium real**: **31/31** -- `pase.html` abierta sin sesion dibuja el QR, dice para quien es y la mesa, y **no muestra el correo del titular ni el id de la reservacion**; con la firma cambiada enseña "este enlace no sirve" y ningun QR; en la puerta el campo del codigo **nace bloqueado**, se desbloquea al registrar la identificacion con su cuenta atras, el escaneo dice "Adelante · 1 de 4 adentro", el codigo se limpia y **vuelve a pedir identificacion**, el segundo intento dice "ese pase ya se uso", y la contingencia se emite y se pasa al campo del codigo con un boton. Cero errores de consola propios. `eslint src` 0 errores; `redocly lint` valido (230 operaciones). **Las 3 fallas de la primera corrida en navegador fueron de la propia prueba** (esperaba español con el navegador en ingles, y una carrera al esperar "que el titulo cambie"), no del sistema. | rama fase5/inventario-por-barra |

| 2026-09-14 | Anticipo por noche (enmienda a D17) | El anticipo del 30% se queda como regla del club, y ahora se puede **sobrescribir por noche**, igual que ya se podia el precio de zona. Migracion `020`: `events_calendar.deposit_pct` NUMERIC(5,2) nullable con CHECK 0-100. Un solo resolvedor en `services/event-pricing.js` (`depositPctFor` / `depositFor`) que usan **los dos** sitios que antes calculaban el anticipo a mano -- la disponibilidad y la cotizacion -- para que no puedan volver a discrepar. `NULL` = la regla del club; **`0` = esta noche se aparta sin cobrar**, que es otra cosa: de ahi el `== null` en vez de un `||`, porque con `||` un cero legitimamente capturado caeria a la regla del club. El PATCH del evento avisa en `X-Warning` cuando se cambia el anticipo (o el cover) habiendo mesas ya apartadas, porque las dos cifras se congelan en la reservacion y NO se recalculan: un gerente que sube el anticipo al 100% para una noche grande supondria que las doce mesas vendidas ahora deben el resto, y no. En la pantalla del gerente, un campo que **nace vacio** con la leyenda de que vacio es el anticipo del club. **Un defecto atrapado por las pruebas:** `getEventPricing` selecciona columnas nombradas y no traia `deposit_pct`, asi que la sobrescritura se ignoraba EN SILENCIO y la cotizacion cobraba el anticipo del club. | **1,586 pruebas verdes** (50 suites; **27 nuevas**). Del resolvedor: la noche manda sobre el club, `null` cae al club, **cero es cero**, NUMERIC que llega como cadena ('30.00') no rompe la cuenta, y nunca sale NaN. De la ruta: se guarda, se cambia, se devuelve a `null`, 101 y -1 se rechazan, y solo el gerente lo toca. Del cobro: la disponibilidad y la cotizacion enseñan el de la NOCHE, el monto queda **congelado** en la reservacion cuando el gerente lo baja despues, el asiento del libro se crea por ese mismo monto, y el aviso sale solo cuando de verdad cambio algo (poner 30 donde ya habia 30 no avisa -- comparacion numerica, no de texto, que es la trampa de `'30.00'` vs `30` documentada en CLAUDE.md). De la pantalla: vacio **no se manda**, el cero si, y un 150% se detiene antes de salir del navegador. `eslint src` 0 errores; `redocly lint` valido. | rama fase5/inventario-por-barra |

| 2026-09-14 | 3.x (entrar con una cuenta de otro) | **Acceso con Facebook, y la verdad sobre Instagram.** Migracion `021`: `user_identities` (una cuenta social por persona y por proveedor, con los dos unicos que evitan que dos cuentas del club queden ligadas al mismo Facebook y que la segunda se robe la sesion de la primera), `oauth_states` (el state en TABLA y de un solo uso: se consume con `DELETE ... RETURNING`, porque un state firmado se puede reproducir tantas veces como quepa en su vigencia), `oauth_handoffs` (el pase que entrega la sesion, 2 minutos, un uso) y `users.password_hash` **nullable** -- NULL dice la verdad: esa cuenta no entra por contrasena, en vez de inventarle un hash que parezca una credencial y no lo sea. `config/oauth.js` + `services/oauth.js` + 8 endpoints en `routes/auth.js`, y en el navegador `web/js/social-login.js` con los botones de la pantalla de acceso, el formulario de completar y el panel de cuentas ligadas del perfil. **Instagram esta declarado y APAGADO con el motivo escrito**: "Iniciar sesion con Instagram" ya no existe para cuentas personales -- la Instagram Basic Display API se apago el 4 de diciembre de 2024 y sus dos sucesores solo aceptan cuentas Business o Creator y ninguno entrega el correo -- asi que un cliente con su Instagram normal no puede entrar por ahi y no hay nada que programar que lo arregle; la ruta contesta **501 con el motivo** en vez de mandarlo a una pantalla de Meta que lo va a rechazar. La misma estructura acepta Google y Apple con solo poner credenciales. Tres reglas que no se negocian (ver D41): la cuenta social **no se liga sola** a una que ya existe aunque coincida el correo, **no se crea cuenta sola** sin fecha de nacimiento y terminos, y la sesion **nunca viaja en la direccion** (pase de mano en `#h=`, no tokens). El token de Facebook se usa una vez y se descarta: no hay columna donde guardarlo. Sin credenciales en el `.env`, el boton no se pinta y la ruta contesta 501 diciendo que variable falta -- correo y contrasena siguen funcionando igual. | **1,704 pruebas verdes** (52 suites; **118 nuevas**). Del servidor (`oauth.test.js`, 58): el state es de un solo uso y no vale uno inventado, de otro proveedor ni vencido; **NO** se liga sola una cuenta que ya existe por el correo; el mismo Facebook en otro club no entra a la cuenta del primero; una cuenta bloqueada no entra; el pase de mano se canjea una sola vez; un **menor** no termina el registro; el token de completar **no sirve como Bearer** (emisor distinto) y una sesion no sirve para completar; no se desvincula la unica manera de entrar; Instagram contesta 501 con su motivo; la lista publica **no lleva el app_secret ni el app_id**; el error de Meta no se reenvia al cliente; una cuenta sin contrasena contesta 401 y no un 500. Del navegador (`web-social-login.test.js`, 60): los ids que busca el controlador existen, el pase **se borra del historial** con `replaceState` en la misma operacion en que se lee, y los textos estan en los dos idiomas. `redocly lint openapi.yaml` valido con las 7 operaciones nuevas. Alta de la app de Meta documentada en `docs/FACEBOOK_LOGIN_SETUP.md`. | (pendiente de commit) |

| 2026-09-14 | 2.x (entradas de mercancia y surtido de barras) | **La mercancia entra en lote y con proveedor; la barra pide y el almacen surte.** Lo que faltaba no era el movimiento -- `receive` y `transfer` ya estaban desde la 018 -- era el CONTEXTO y el LOTE. Migracion `022`: `suppliers` (unico por `lower(name)`, porque sin eso en tres meses hay "Corona", "corona sa" y "CORONA S.A." y la pregunta "cuanto le compro a Corona" deja de tener respuesta; baja logica, nunca borrado), `supply_suppliers` (quien surte que, con `last_cost` de la presentacion para notar que subio el precio y para precargar la captura), `bar_requests` + `bar_request_lines` (lo pedido Y lo surtido por renglon, que es lo unico que despues contesta "pedi doce y me llegaron ocho"), y tres columnas nuevas en `supply_movements`: `supplier_id`, `receipt_group` y `request_id`. **Entrada en lote** (`POST /supply-receipts`): N renglones en UNA transaccion, costo POR LINEA -- cada renglon mueve el promedio ponderado de SU insumo -- y el insumo repetido se rechaza. **Surtido**: la barra crea el pedido desde `bartender.html` con lo que esta bajo minimo ya marcado y la cantidad sugerida redondeada hacia arriba; el almacen lo surte desde `almacen.html` (dos pestanas nuevas: Entrada y Pedidos) contra el almacen o contra la otra barra. Surtir de MENOS deja el pedido en `partial` y devuelve el faltante; surtir de MAS se niega. Web: `web/js/receiving.js` con toda la aritmetica del borrador (cajas a mililitros, costo de presentacion a costo de unidad base, total, y la vista previa del estado del pedido), aparte para poder probarla sin navegador. **Dos defectos atrapados en el camino:** (1) sellar el folio con un `UPDATE` sobre `supply_movements` fallaba contra el disparador de solo-insercion -- la restriccion es la correcta, asi que el folio y el proveedor pasaron al INSERT (`inventory.move` los acepta ahora); (2) `UPDATE bar_requests SET status = $2 ... CASE WHEN $2 = 'fulfilled'` reventaba con `42P08`, la primera trampa de CLAUDE.md, y se arreglo casteando `$2::text` en las DOS apariciones. Y un tercero del lado del navegador: `Number('')` es 0, asi que un renglon sin cantidad se leia como "entraron cero" en vez de "falta capturarlo". | **1,881 pruebas verdes** (55 suites; **177 nuevas**). Servidor (`receiving.test.js` 29, `bar-requests.test.js` 33): el lote entra completo o no entra y nombra TODAS las lineas malas con su numero de renglon; el costo por linea mueve cada promedio por separado y se pondera por cantidad (20 a $900 y 1 a $1,400 dan 0.9238, no 1.15); el insumo repetido se rechaza; el proveedor no se duplica por mayusculas; el almacen ve el catalogo pero no da de alta; surtir de menos manda lo que hay y deja el faltante escrito; dos parciales completan; surtir de mas se niega; una barra no se surte a si misma y el cantinero no se surte a si mismo; un pedido cancelado o completo no se vuelve a surtir; lo ya surtido antes de cancelar NO se devuelve. Navegador (`web-receiving.test.js` 115): la aritmetica del borrador, los `id` que cada controlador busca, los textos en los dos idiomas y la vista previa del estado. `redocly lint` valido con **13 operaciones nuevas**. Y prueba de extremo a extremo en Chromium real: el almacen captura tres renglones ($9,672, existencias 4,500/12,000/8,520 ml y promedios 1.2/0.32/0.0507), la barra pide con lo bajo minimo ya marcado (4 botellas), y el almacen surte: whisky 1,500 en almacen y 3,000 en barra, pedido `fulfilled`, los dos renglones del traspaso ligados al pedido, un solo folio con proveedor en los tres. Sin un solo error de JavaScript. | (pendiente de commit) |

| 2026-09-15 | 3.x (el gerente: corte de la noche y rol) | **Aclaracion primero: el rol de gerente ya existia** -- esta en la base desde la 001, tiene `manager.html` con nueve pestanas, y `almacen.html` ya lo aceptaba (`WAREHOUSE_ROLES` incluye `manager`). Lo que faltaba eran las otras dos cosas. Migracion `023`: **`night_closings`** (el corte CONGELADO al cerrar, solo-insercion con disparador que rechaza UPDATE y DELETE, unico por evento, cifras comparables en columnas y los bloques de largo variable en `jsonb`) y **`shift_assignments`** (el rol de la noche: una fila por persona-noche-destino, `section` para meseros y hostess con VARIOS destinos, `location_id` para bartenders con UNO, excluyentes por CHECK, con disparador que impide cruzar clubes y asignar a alguien al almacen). `services/night-stats.js` con un bloque por consulta -- reservaciones, zonas, barra, puerta, propinas, merma, personal -- porque un JOIN de todo contra todo multiplica filas y ese error no revienta, solo da un numero mas grande. `services/assignments.js` con las reglas y los HUECOS (`gaps`), que son la mitad del valor: una zona sin mesero no se nota mirando quien esta asignado. `routes/nights.js` con 7 rutas. En el navegador, `web/js/night-report.js` (las cuatro cifras de arriba, los avisos con su numero, y el comparador) y `web/js/roster.js` (candidatos, validacion y agrupado por destino), los dos aparte para probarlos sin navegador; dos hojas nuevas en `manager.html` colgadas de cada noche (Corte y Rol, porque el gerente piensa "como salio el viernes", no "abre el reporte"); y el empleado ve su lugar en `employee-portal.html` antes de marcar entrada. **Tres reglas que no se negocian (D43):** cancelado no es vendido y no-show no es ocupado; las propinas NO son ingreso del club y viajan como `tips_not_club_revenue`; la merma se valua al costo y la cortesia va aparte. Y **asignar no abre el turno**: es lo unico que deja ver quien no llego. **Ademas (D44): la integracion con SoftRestaurant11 se posterga** por decision del dueno -- el POS de este sistema es la fuente de la venta, asi que el corte presenta el total real del club sin advertencias. | **2,070 pruebas verdes** (58 suites; **189 nuevas**). Servidor (`night-stats.test.js` 33, `assignments.test.js` 36): una noche sin actividad da ceros y las zonas vacias SI aparecen; cancelada no cuenta como mesa usada ni como venta; una confirmada que nadie marco queda como apartada sin inventar el estado; el pedido cancelado y el que se quedo en preparacion no son venta; las propinas no entran al total; la merma se valua al costo y la cortesia se reporta aparte; el ticket promedio con cero asistencia es 0 y no una division por cero; **el corte NO cambia si despues cambia un precio**; una noche se cierra una vez; una noche que no empieza no se puede cortar; el corte guardado no se puede editar ni borrar. Del rol: un mesero cubre dos zonas pero no la misma dos veces, un bartender no queda en dos barras, nadie se asigna a si mismo, **asignar no abre turno**, quitar del rol no cierra el turno de quien llego, un dado de baja no entra, uno sin ficha de empleado si, y el puesto queda congelado. Navegador (`web-night-report.test.js` 120): los `id`, los textos en los dos idiomas, el porcentaje sin denominador que devuelve null y no cero, y que **las reglas del cliente y del servidor son las mismas** (si se separan, el gerente ve un boton que el servidor va a rechazar). `redocly lint` valido con **7 operaciones nuevas**. Y prueba de extremo a extremo en Chromium real: el gerente arma el rol (los candidatos salen filtrados por puesto -- solo el mesero para una zona, solo el barman para una barra), el progreso pasa de "faltan 2 zonas y 2 barras" a "falta 1 y 1", **cero turnos abiertos**, y el corte ensena $9,000 (mesas + puerta, propinas fuera), 28 asistentes, 50% de mesas y $321.43 por persona, con los tres avisos correctos; al cerrarlo, el UPDATE al corte queda rechazado. Sin un solo error de JavaScript. | (pendiente de commit) |

| 2026-09-16 | 3.x (admin, la puerta del almacen y la foto del ticket) | **Cuatro cosas que pidio el dueno.** (1) **Tu cuenta a admin, y crear gerentes.** `scripts/promote.js` -- `npm run promote -- --email x --role admin` -- es el unico camino al rol `admin`, y esta donde debe estar: en la consola del servidor. Deja el cambio en `audit_log`, se niega con una cuenta de cliente (`guest`) y con una bloqueada, y pide `--club` si el correo existe en dos clubes. En la app, `POST /employees` ya acepta `manager` **pero solo si quien lo pide es admin**; `admin` no se puede dar por HTTP ni siendo admin. Se cerraron las cuatro puertas, no solo el alta: cambiarle el rol a un gerente, darlo de baja y **reiniciarle la contrasena** (que es tomarle la cuenta) tambien quedaron solo para el admin. (2) **La puerta al almacen** -- era un defecto real y mio: `almacen.html` no estaba enlazado desde ninguna parte del panel, asi que el gerente tenia que teclear la URL. Ahora hay dos: el icono de cajas en el encabezado y un boton dentro de la pestana Inventario; y desde el almacen, la vuelta al panel, escondida para quien no tiene panel. (3) **La foto del ticket o la factura** (D45). Migracion `024`: `receipt_photos` (solo-insercion), `pg_trgm` y `ev2_norm()`. `POST /receipt-photos` guarda la imagen en un volumen y **luego** la lee con **Tesseract** en el servidor; lo leido es una PROPUESTA que no mueve una sola existencia. Cada renglon comprueba **cantidad x precio = importe**, y lo que no cuadra sale marcado en vez de entrar en silencio. El insumo se propone por parecido de nombre, y con dos parecidos se deja vacio. Una foto se usa una vez (comprobado dentro de la transaccion de la captura: el doble toque **no** mete la entrega dos veces) y nunca se borra. (4) **La pantalla**: boton de camara en la pestana Entrada, los renglones dudosos primero, el texto leido junto a cada renglon para comparar con el papel, el visor de la foto con el token puesto, y el aviso cuando la suma no cuadra con el subtotal impreso -- que es lo unico que delata un renglon que el lector se comio por completo. | `server/scripts/promote.js`, `server/migrations/024_receipt_photos.sql`, `server/src/services/receipt-ocr.js`, `server/src/routes/receipts.js`, `server/src/routes/employees.js`, `server/src/routes/inventory.js`, `server/src/app.js`, `server/package.json`, `web/js/receipt-review.js`, `web/js/staff-admin.js`, `web/js/manager-screen.js`, `web/js/warehouse-screen.js`, `web/js/receiving.js`, `web/js/api.js` (multipart y blob), `web/js/format.js`, `web/manager.html`, `web/almacen.html`, `deploy/Dockerfile.api` (tesseract-ocr + spa), `deploy/docker-compose.prod.yml` y `deploy/docker-compose.yml` (volumen `receipt_photos`), `server/openapi.yaml`, `docs/DECISIONES.md` (D45) | **2,168 pruebas verdes** (`npm test`, en serie, contra Postgres real). Nuevas: `promote.test.js` 12, `receipt-ocr.test.js` 32 -- varias contra una **imagen real** (`tests/fixtures/factura-proveedor.jpg`, con ruido y torcida, como sale del telefono) --, `receipt-photos.test.js` 21 de punta a punta con multipart, `web-receipt-review.test.js` 22, y 7 nuevas de la gerencia en `employees.test.js`. Ademas `docs/api/smoke-test-foto-ticket.sh` (**27 comprobaciones**) contra la API viva, y una prueba en **navegador real** (Chromium, 420x900) sobre `almacen.html`: 23 de 23. `redocly lint` limpio; `eslint` sin errores; migracion 024 aplicada desde cero. |

**Pendiente del dueno antes de 3.4/3.5:** cuentas de Stripe y de Mercado Pago (alta del negocio, datos fiscales y llaves de prueba). Sin ellas se puede llegar hasta 3.3.

**Hoja de control (seccion 15 del manual):** `docs/CONTROL_AVANCE.md`. Fases 0-2 y el hito H1 registrados; faltan las horas reales y las firmas, que llena Erick.
