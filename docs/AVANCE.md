# Bitácora de avance — EV2 Clandestinoz

Cada fila se agrega al terminar y verificar un paso del manual. Formato: fecha · paso · qué se hizo · verificación · commit/PR.

| Fecha | Paso | Qué se hizo | Verificación | Commit / PR |
|---|---|---|---|---|
| 2026-09-03 | — | Plan (`PLAN_IMPLEMENTACION_Y_DESARROLLO.md`) y manual (`MANUAL_IMPLEMENTACION_EV2.pdf`) creados. `CLAUDE.md` y esta bitácora creados. | Archivos presentes en la carpeta. | (sin git aún) |
| 2026-09-03 | 0.1 (parcial) | `.gitignore`, `AUTHORS.md`, `git init -b main`, autor Erick Lopez, primer commit `66d5e7f` (129 archivos). `.env` y `Thumbs.db` excluidos. | `git status` limpio · `git ls-files \| grep env` → solo `.env.example` · `git fsck` sin errores. Pendiente: remoto privado y push. | 66d5e7f (local) |
| 2026-09-03 | 0.1 (cierre) | Remoto `origin` = https://github.com/Jovannyrdmx/ev2-app.git; push de `main` realizado desde la computadora del desarrollador. | `git branch -vv` → `main [origin/main]` sincronizada en `bfef436`. **Paso 0.1 completo.** | bfef436 |
| 2026-09-03 | 0.2 | Reorganización a `server/ web/ mobile/ deploy/ docs/` con `git mv` (rama `fase0/reorganizar`, commit `253c35d`). `README.md` nuevo. Sin cambios de contenido en código. | `git status` limpio · 123 renombrados, 5 nuevos (README + 4 .gitkeep), 0 borrados, 0 modificados · `git log --follow` conserva historial · raíz solo con README, CLAUDE, AUTHORS, .gitignore, .env.example y carpetas. | 253c35d (rama fase0/reorganizar) |
| 2026-09-03 | 0.3 | `docs/DECISIONES.md` con 15 decisiones (qué, por qué, alternativas descartadas) y reglas absolutas; enlazado desde `README.md`. | Archivo existe y `grep DECISIONES README.md` lo muestra. | rama fase0/decisiones |
| 2026-09-03 | 0.4 | `server/package.json` reescrito (nombre `ev2-server`, scripts reales, engines node>=18, jest config); dependencias añadidas: stripe, mercadopago, dotenv, helmet, express-rate-limit, zod, pino, pino-http; dev: jest, supertest, eslint 9 (+@eslint/js, globals), nodemon. `eslint.config.js` creado. `package-lock.json` generado (508 paquetes). | `npm ls --depth=0` sin errores. `npm run lint`: 26 errores, todos en `src/routes/reservations.js` (`pool`, `authenticateToken`, `broadcastToNightclub` no definidos: confirma que nunca se integró; se corrige en 1.4) + 14 avisos de variables sin usar en código heredado. | rama fase0/package |
| 2026-09-03 | 0.5 | `server/src/index.js`: cliente Redis con sintaxis v4 (`socket: {host, port}` + `reconnectStrategy`), `dotenv`, función `start()` que conecta Redis y verifica Postgres antes de escuchar (sale con código 1 si no hay conexión), apagado limpio con SIGTERM/SIGINT, y `module.exports` de app/server/pool para tests. | `node --check` OK · el módulo carga y exporta app/server/pool/redisClient/start · sin Redis el arranque falla limpio con exit 1 · eslint 0 errores en el archivo. Conexión real se comprueba en 0.7. | rama fase0/redis |
| 2026-09-03 | 0.6 | `deploy/`: `Dockerfile.api` y `Dockerfile.ws` reescritos (node:20-alpine, `npm ci --omit=dev`, copia `src/` y `migrations/` completos, usuario `node`, healthcheck); `Dockerfile.web` con nginx sirviendo `web/` + `nginx-web.conf` (cabeceras de seguridad, gzip); `Dockerfile.legacy` eliminado; `docker-compose.yml` nuevo (contextos correctos, Postgres/Redis sin puertos al host, `DB_PASSWORD` y `JWT_SECRET` obligatorios, sin initdb, nginx bajo `--profile proxy`); `docker-compose.override.example.yml` para exponer BD en desarrollo; `.dockerignore` en raíz y `server/`; `.env.example` con `DB_NAME=ev2` y secretos marcados como obligatorios. | `docker compose config` valida (5 servicios). No fue posible construir imágenes en la nube (registro Docker bloqueado): en su lugar se ejecutó el código real contra Postgres 16 y Redis 7 nativos: `Redis connected`, `PostgreSQL connected`, `/health` API y WS → 200, SIGTERM apaga limpio. El `docker compose build/up` se confirma en la PC del desarrollador en 0.7. | rama fase0/docker |
| 2026-09-03 | 0.7 (en curso) | Error en la PC: `DB_PASSWORD is required`. Causa: con `-f deploy/docker-compose.yml`, Compose busca `.env` en `deploy/`, no en la raíz. Corrección: usar `--env-file .env`; README y cabecera del compose actualizados. | Pendiente: build/up en la PC del desarrollador. | rama fase0/compose-envfile |
| 2026-09-03 | 0.7 | `docker compose --env-file .env -f deploy/docker-compose.yml build` y `up -d` ejecutados en la PC del desarrollador (Windows, Docker Desktop). | `http://localhost:3000/health` → 200 `{"status":"ok"}` · `http://localhost:4000/health` → 200 `{"status":"ok","clients":0}` · `http://localhost:8080/` → 200. La API arrancó, lo que implica conexión real a Postgres y Redis dentro de Docker. `compose ps`: postgres, redis, api, ws **healthy**; web `unhealthy` pese a responder 200 → healthcheck con `localhost` (IPv6) contra nginx solo IPv4. Corregido: `listen [::]:8080` + healthchecks con `127.0.0.1`. Rebuild de `web` en la PC: healthy. **Paso 0.7 completo.** | main |
| 2026-09-03 | 0.8 | `.github/workflows/ci.yml`: job `server` (Node 20, `npm ci`, lint con `continue-on-error` temporal hasta 1.4, `npm test --passWithNoTests`) y job `docker` (build de las 3 imágenes + `compose config` con secretos ficticios). Badge de CI en README. | Pendiente: primer run verde en GitHub Actions tras el push. | rama fase0/ci |
| 2026-09-03 | 0.9 (en curso) | Investigación pública: National Soft expone "SoftRestaurant Service" (REST, header `AuthorizedApp`, solo menú, llave por correo), programa oficial de integración para terceros (Deliverect/Hubster/Ordatic envían órdenes), add-ons e-Delivery/SR Móvil, módulo ERP/PMS (PDF), y SR11 corre sobre SQL Server. El cliente heredado asume rutas inexistentes. Borrador en `docs/POS_REAL.md` con escenarios A–E y recomendación (A comandas + D lectura SQL). | Pendiente: datos del dueño (versión, licencias, credenciales SQL/API), envío del formulario a National Soft, lectura del PDF ERP/PMS. | rama fase0/pos-real |
| 2026-09-03 | 0.9 (cierre) | Confirmado con el desarrollador: SR **11.0.98**, SQL Server en servidor `192.168.2.108` (LAN del club, acceso por Radmin VPN), PC del desarrollador = cliente contra datos reales, sin ambiente de pruebas, conexión cifrada en `conexionDB.dbl` (no se toca). `docs/POS_REAL.md` cerrado para Fase 0; **D16 agente local** añadida a `docs/DECISIONES.md`. Pendientes no bloqueantes: formulario a National Soft, PDF ERP/PMS, add-ons, nombre de instancia/base, administrador del servidor. **FASE 0 COMPLETA.** | `docs/POS_REAL.md` secciones 3–5; D16 en DECISIONES. | rama fase0/pos-real-final |
| 2026-09-03 | 1.1 | `server/migrations/001_init.sql`: esquema unificado, **38 tablas** (núcleo, mesas, bebidas/pedidos, flirt y seguridad, finanzas con libro `transactions` inmutable, propinas/canciones/turnos, reservaciones con exclusión de solapes, taxi/valet, precios/POS/eventos), extensiones pgcrypto y btree_gist, triggers `updated_at`, `forbid_delete`, guardia de columnas inmutables y cálculo de `ends_at`. Diseño aprobado por el dueño antes de escribir. | Aplicada en PostgreSQL 16 limpio sin errores. Pruebas: solape de reservaciones rechazado; cambio de estado en `transactions` OK y cambio de monto/borrado rechazados; segunda ocupación abierta rechazada; flirt a sí mismo y rol inválido rechazados. | rama fase1/esquema |

## Próximo paso

Confirmar CI verde en GitHub (0.8). Llenar hoja de control de la Fase 0 (manual, sección 15). Después: **1.2 — Runner de migraciones y seeds** (6 h).
